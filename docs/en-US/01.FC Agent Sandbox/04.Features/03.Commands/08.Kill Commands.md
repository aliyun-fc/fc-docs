# Kill Commands

Killing a command stops a background process or PTY session that is running in the sandbox. It does not terminate the entire sandbox.

## Kill a background command

```typescript
const handle = await sandbox.commands.run("sleep 600", {
  background: true,
  timeoutMs: 10 * 60 * 1000
});

const killed = await sandbox.commands.kill(handle.pid);
console.log(killed);
```

Python example:

```python
handle = sandbox.commands.run(
    "sleep 600",
    background=True,
    timeout=10 * 60,
)

killed = sandbox.commands.kill(handle.pid)
print(killed)
```

## Clean up background commands

```typescript
const commandPids = [handle.pid];

for (const pid of commandPids) {
  await sandbox.commands.kill(pid);
}
```

Python example:

```python
command_pids = [handle.pid]

for pid in command_pids:
    sandbox.commands.kill(pid)
```

Your application should persist the `pid` of the background commands it starts. `sandbox.commands.list()` returns running commands and PTY sessions. Use `sandbox.commands.kill(pid)` for regular commands and `sandbox.pty.kill(pid)` for PTY sessions. Distinguish them by process purpose or the `pid` values you persisted, so you do not treat interactive sessions as ordinary background commands.

## Recommendations

Long-running tasks should have application-side timeouts and actively kill commands after a timeout is reached. Killing a command only releases process resources. When the full task is finished, also call `sandbox.kill()` to release the sandbox.

## HTTP request and response fields

The following protocol details were checked against the official E2B OpenAPI on 2026-09-08. Required properties apply to their containing object. FC support and differences follow the guidance on this page and the [compatibility inventory](../../07.Developer%20Reference/01.E2B%20SDK-compatible%20API%20List.md). Upstream defaults, status codes and new fields do not establish FC behavior.

### `POST /process.Process/SendSignal`



[Official contract](https://docs.e2b.dev/api-reference/process/sendsignal)

**Endpoint:** `POST /process.Process/SendSignal`

Servers: `https://sandbox.{domain}`.

Security (upstream OpenAPI): `[{"SandboxAccessTokenAuth": []}]`. Scheme details are provided in the linked official contract.

#### Request parameters

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| E2b-Sandbox-Id | header | string | yes | — | Identifier of the target sandbox. Routes the request to that sandbox's envd over the shared sandbox host. |
| E2b-Sandbox-Port | header | integer | yes | default=49983 | Port envd listens on inside the sandbox (default 49983). |
| Connect-Protocol-Version | header | connect-protocol-version | yes | const=1 | Define the version of the Connect protocol |
| Connect-Timeout-Ms | header | connect-timeout-header | no | — | Define the timeout, in ms |

#### Request body

Required: **yes**.

##### application/json

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `process` | process.ProcessSelector | no | — | — |
| `signal` | process.Signal | no | enum=["SIGNAL_UNSPECIFIED", "SIGNAL_SIGTERM", "SIGNAL_SIGKILL"] | — |

#### Responses

| Status | Description | Content type | Schema |
|---|---|---|---|
| 200 | Success | application/json | process.SendSignalResponse |
| 502 | Sandbox not found | application/json | object |

#### Data structures

##### connect-protocol-version

Define the version of the Connect protocol

Type: `number`. Constraints: const=1. See the official contract linked above.

##### connect-timeout-header

Define the timeout, in ms

Type: `number`. Constraints: —. See the official contract linked above.

##### process.SendSignalRequest

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `process` | process.ProcessSelector | no | — | — |
| `signal` | process.Signal | no | enum=["SIGNAL_UNSPECIFIED", "SIGNAL_SIGTERM", "SIGNAL_SIGKILL"] | — |

##### process.SendSignalResponse

Type: `object`. Constraints: —. See the official contract linked above.

##### process.ProcessSelector

Type: `oneOf: see schema, see schema`. Constraints: —. See the official contract linked above.

##### process.Signal

Type: `string`. Constraints: enum=["SIGNAL_UNSPECIFIED", "SIGNAL_SIGTERM", "SIGNAL_SIGKILL"]. See the official contract linked above.

# Watch for Changes

Filesystem supports directory change watching. This is useful for waiting for agent output files in long tasks, syncing artifacts generated by programs inside the sandbox, or forwarding file change events to a business-side state machine.

## Watch a directory

TypeScript example:

```typescript
const watcher = await sandbox.files.watchDir("/tmp/output", (event) => {
  console.log(event);
});

// Stop watching after the task finishes.
await watcher.stop();
```

`onEvent` is the second positional argument to `watchDir()`, not a field inside an options object. Optional settings such as `recursive` and `onExit` are passed through the third argument. Stop watching with `watcher.stop()`.

Python example:

The callback-based `watch_dir()` API is asynchronous. Use `AsyncSandbox` and pass the callback through the `on_event` argument:

```python
import asyncio

from e2b_code_interpreter import AsyncSandbox


async def main():
    sandbox = await AsyncSandbox.create("code-interpreter-v1")
    handle = await sandbox.files.watch_dir(
        "/tmp/output",
        on_event=lambda event: print(event),
    )

    # Stop watching after the task finishes.
    await handle.stop()


asyncio.run(main())
```

Before production use, validate the event shape, close behavior, and error responses with a minimal example.

## Recommendations

- Make sure the directory exists before watching it. You can create the output directory with `sandbox.files.makeDir()`.
- The callback may receive duplicate events. Deduplicate on the business side by file path, event type, and task ID.
- Watching improves timeliness, but final task state should still be determined from the command exit code, a business state file, or external task state.
- For long-running watchers, set a business timeout and stop the watcher when the task succeeds, fails, or times out.
- If you only need to wait for a single file to appear, you can still use bounded polling with `sandbox.files.exists()`.


## Watcher options and version requirements

| Option | Default and constraint |
| --- | --- |
| `recursive` | false; watch recursively when enabled. |
| `include_entry` | false; includes entry metadata when available, requiring envd ≥ 0.6.3. |
| `allow_network_mounts` | false; network mount watching requires envd ≥ 0.6.4 and events may be unreliable. |
| `user`, `request_timeout` | Execution user and request timeout in seconds. |

Python `watch_dir()` returns `WatchHandle`; use `get_new_events()` to read new events and `stop()` to stop watching. Verify the FC template's envd version before enabling newer options.

[Python SDK 2.37.1 reference](https://docs.e2b.dev/sdk-reference/python-sdk/v2.37.1/sandbox_sync)

## HTTP request and response fields

The following protocol details were checked against the official E2B OpenAPI on 2026-09-08. Required properties apply to their containing object. FC support and differences follow the guidance on this page and the [compatibility inventory](../../07.Developer%20Reference/01.E2B%20SDK-compatible%20API%20List.md). Upstream defaults, status codes and new fields do not establish FC behavior.

### `POST /filesystem.Filesystem/CreateWatcher`



[Official contract](https://docs.e2b.dev/api-reference/filesystem/createwatcher)

**Endpoint:** `POST /filesystem.Filesystem/CreateWatcher`

Servers: `https://sandbox.{domain}`.

Security (upstream OpenAPI): `[{"SandboxAccessTokenAuth": [], "SandboxUserAuth": []}]`. Scheme details are provided in the linked official contract.

#### Request parameters

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| E2b-Sandbox-Id | header | string | yes | — | Identifier of the target sandbox. Routes the request to that sandbox's envd over the shared sandbox host. |
| E2b-Sandbox-Port | header | integer | yes | default=49983 | Port envd listens on inside the sandbox (default 49983). |
| Connect-Protocol-Version | header | connect-protocol-version | yes | const=1 | Define the version of the Connect protocol |
| Connect-Timeout-Ms | header | connect-timeout-header | no | — | Define the timeout, in ms |

#### Request body

Required: **yes**.

##### application/json

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `path` | string | no | — | — |
| `recursive` | boolean | no | — | — |
| `includeEntry` | boolean | no | — | If true, each FilesystemEvent includes the EntryInfo of the affected entry, when available. |
| `allowNetworkMounts` | boolean | no | — | If true, allows watching paths on network filesystem mounts (NFS, CIFS, SMB, FUSE).  Events on network mounts may be unreliable or not delivered at all. |

#### Responses

| Status | Description | Content type | Schema |
|---|---|---|---|
| 200 | Success | application/json | filesystem.CreateWatcherResponse |
| 502 | Sandbox not found | application/json | object |

#### Data structures

##### connect-protocol-version

Define the version of the Connect protocol

Type: `number`. Constraints: const=1. See the official contract linked above.

##### connect-timeout-header

Define the timeout, in ms

Type: `number`. Constraints: —. See the official contract linked above.

##### filesystem.CreateWatcherRequest

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `path` | string | no | — | — |
| `recursive` | boolean | no | — | — |
| `includeEntry` | boolean | no | — | If true, each FilesystemEvent includes the EntryInfo of the affected entry, when available. |
| `allowNetworkMounts` | boolean | no | — | If true, allows watching paths on network filesystem mounts (NFS, CIFS, SMB, FUSE).  Events on network mounts may be unreliable or not delivered at all. |

##### filesystem.CreateWatcherResponse

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `watcherId` | string | no | — | — |



### `POST /filesystem.Filesystem/GetWatcherEvents`



[Official contract](https://docs.e2b.dev/api-reference/filesystem/getwatcherevents)

**Endpoint:** `POST /filesystem.Filesystem/GetWatcherEvents`

Servers: `https://sandbox.{domain}`.

Security (upstream OpenAPI): `[{"SandboxAccessTokenAuth": []}]`. Scheme details are provided in the linked official contract.

#### Request parameters

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| E2b-Sandbox-Id | header | string | yes | — | Identifier of the target sandbox. Routes the request to that sandbox's envd over the shared sandbox host. |
| E2b-Sandbox-Port | header | integer | yes | default=49983 | Port envd listens on inside the sandbox (default 49983). |
| Connect-Protocol-Version | header | connect-protocol-version | yes | const=1 | Define the version of the Connect protocol |
| Connect-Timeout-Ms | header | connect-timeout-header | no | — | Define the timeout, in ms |

#### Request body

Required: **yes**.

##### application/json

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `watcherId` | string | no | — | — |

#### Responses

| Status | Description | Content type | Schema |
|---|---|---|---|
| 200 | Success | application/json | filesystem.GetWatcherEventsResponse |
| 502 | Sandbox not found | application/json | object |

#### Data structures

##### connect-protocol-version

Define the version of the Connect protocol

Type: `number`. Constraints: const=1. See the official contract linked above.

##### connect-timeout-header

Define the timeout, in ms

Type: `number`. Constraints: —. See the official contract linked above.

##### filesystem.GetWatcherEventsRequest

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `watcherId` | string | no | — | — |

##### filesystem.GetWatcherEventsResponse

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `events` | array of filesystem.FilesystemEvent | no | — | — |

##### filesystem.FilesystemEvent

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `name` | string | no | — | — |
| `type` | filesystem.EventType | no | enum=["EVENT_TYPE_UNSPECIFIED", "EVENT_TYPE_CREATE", "EVENT_TYPE_WRITE", "EVENT_TYPE_REMOVE", "EVENT_TYPE_RENAME", "EVENT_TYPE_CHMOD"] | — |
| `entry` | oneOf: filesystem.EntryInfo, null | no | — | Info of the entry that triggered the event. Only populated when include_entry  was requested and the entry could be stat-ed (e.g. not set for remove/rename-away  events, where the entry no longer exists at this path). |

##### filesystem.EventType

Type: `string`. Constraints: enum=["EVENT_TYPE_UNSPECIFIED", "EVENT_TYPE_CREATE", "EVENT_TYPE_WRITE", "EVENT_TYPE_REMOVE", "EVENT_TYPE_RENAME", "EVENT_TYPE_CHMOD"]. See the official contract linked above.

##### filesystem.EntryInfo

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `name` | string | no | — | — |
| `type` | filesystem.FileType | no | enum=["FILE_TYPE_UNSPECIFIED", "FILE_TYPE_FILE", "FILE_TYPE_DIRECTORY", "FILE_TYPE_SYMLINK"] | — |
| `path` | string | no | — | — |
| `size` | integer / string | no | format="int64" | File size in bytes. Encoded as string for values exceeding JSON number precision (int64). |
| `mode` | integer | no | — | — |
| `permissions` | string | no | — | — |
| `owner` | string | no | — | — |
| `group` | string | no | — | — |
| `modifiedTime` | google.protobuf.Timestamp | no | format="date-time" | A Timestamp represents a point in time independent of any time zone or local  calendar, encoded as a count of seconds and fractions of seconds at  nanosecond resolution. The count is relative to an epoch at UTC midnight on  January 1, 1970, in the proleptic Gregorian calendar which extends the  Gregorian calendar backwards to year one.   All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap  second table is needed for interpretation, using a [24-hour linear  smear](https://developers.google.com/time/smear).   The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By  restricting to that range, we ensure that we can convert to and from [RFC  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.   # Examples   Example 1: Compute Timestamp from POSIX `time()`.       Timestamp timestamp;      timestamp.set_seconds(time(NULL));      timestamp.set_nanos(0);   Example 2: Compute Timestamp from POSIX `gettimeofday()`.       struct timeval tv;      gettimeofday(&tv, NULL);       Timestamp timestamp;      timestamp.set_seconds(tv.tv_sec);      timestamp.set_nanos(tv.tv_usec * 1000);   Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.       FILETIME ft;      GetSystemTimeAsFileTime(&ft);      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) \| ft.dwLowDateTime;       // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.      Timestamp timestamp;      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));   Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.       long millis = System.currentTimeMillis();       Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)          .setNanos((int) ((millis % 1000) * 1000000)).build();   Example 5: Compute Timestamp from Java `Instant.now()`.       Instant now = Instant.now();       Timestamp timestamp =          Timestamp.newBuilder().setSeconds(now.getEpochSecond())              .setNanos(now.getNano()).build();   Example 6: Compute Timestamp from current time in Python.       timestamp = Timestamp()      timestamp.GetCurrentTime()   # JSON Mapping   In JSON format, the Timestamp type is encoded as a string in the  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"  where {year} is always expressed using four digits while {month}, {day},  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone  is required. A proto3 JSON serializer should always use UTC (as indicated by  "Z") when printing the Timestamp type and a proto3 JSON parser should be  able to accept both UTC and other timezones (as indicated by an offset).   For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past  01:30 UTC on January 15, 2017.   In JavaScript, one can convert a Date object to this format using the  standard  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)  method. In Python, a standard `datetime.datetime` object can be converted  to this format using  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use  the Joda Time's [`ISODateTimeFormat.dateTime()`](  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()  ) to obtain a formatter capable of generating timestamps in this format. |
| `symlinkTarget` | string / null | no | — | If the entry is a symlink, this field contains the target of the symlink. |
| `metadata` | object | no | — | User-defined metadata stored as extended attributes (xattrs) on the file.  Keys live under the `user.e2b.` xattr namespace; the prefix is stripped here.  Plain `user.*` xattrs written by other tooling are not reflected. |

##### filesystem.FileType

Type: `string`. Constraints: enum=["FILE_TYPE_UNSPECIFIED", "FILE_TYPE_FILE", "FILE_TYPE_DIRECTORY", "FILE_TYPE_SYMLINK"]. See the official contract linked above.

##### google.protobuf.Timestamp

A Timestamp represents a point in time independent of any time zone or local
 calendar, encoded as a count of seconds and fractions of seconds at
 nanosecond resolution. The count is relative to an epoch at UTC midnight on
 January 1, 1970, in the proleptic Gregorian calendar which extends the
 Gregorian calendar backwards to year one.

 All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
 second table is needed for interpretation, using a [24-hour linear
 smear](https://developers.google.com/time/smear).

 The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
 restricting to that range, we ensure that we can convert to and from [RFC
 3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.

 # Examples

 Example 1: Compute Timestamp from POSIX `time()`.

     Timestamp timestamp;
     timestamp.set_seconds(time(NULL));
     timestamp.set_nanos(0);

 Example 2: Compute Timestamp from POSIX `gettimeofday()`.

     struct timeval tv;
     gettimeofday(&tv, NULL);

     Timestamp timestamp;
     timestamp.set_seconds(tv.tv_sec);
     timestamp.set_nanos(tv.tv_usec * 1000);

 Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.

     FILETIME ft;
     GetSystemTimeAsFileTime(&ft);
     UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;

     // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
     // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
     Timestamp timestamp;
     timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
     timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));

 Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.

     long millis = System.currentTimeMillis();

     Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
         .setNanos((int) ((millis % 1000) * 1000000)).build();

 Example 5: Compute Timestamp from Java `Instant.now()`.

     Instant now = Instant.now();

     Timestamp timestamp =
         Timestamp.newBuilder().setSeconds(now.getEpochSecond())
             .setNanos(now.getNano()).build();

 Example 6: Compute Timestamp from current time in Python.

     timestamp = Timestamp()
     timestamp.GetCurrentTime()

 # JSON Mapping

 In JSON format, the Timestamp type is encoded as a string in the
 [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
 format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
 where {year} is always expressed using four digits while {month}, {day},
 {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
 seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
 are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
 is required. A proto3 JSON serializer should always use UTC (as indicated by
 "Z") when printing the Timestamp type and a proto3 JSON parser should be
 able to accept both UTC and other timezones (as indicated by an offset).

 For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
 01:30 UTC on January 15, 2017.

 In JavaScript, one can convert a Date object to this format using the
 standard
 [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
 method. In Python, a standard `datetime.datetime` object can be converted
 to this format using
 [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
 the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
 the Joda Time's [`ISODateTimeFormat.dateTime()`](
 http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
 ) to obtain a formatter capable of generating timestamps in this format.

Type: `string`. Constraints: format="date-time". See the official contract linked above.



### `POST /filesystem.Filesystem/RemoveWatcher`



[Official contract](https://docs.e2b.dev/api-reference/filesystem/removewatcher)

**Endpoint:** `POST /filesystem.Filesystem/RemoveWatcher`

Servers: `https://sandbox.{domain}`.

Security (upstream OpenAPI): `[{"SandboxAccessTokenAuth": []}]`. Scheme details are provided in the linked official contract.

#### Request parameters

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| E2b-Sandbox-Id | header | string | yes | — | Identifier of the target sandbox. Routes the request to that sandbox's envd over the shared sandbox host. |
| E2b-Sandbox-Port | header | integer | yes | default=49983 | Port envd listens on inside the sandbox (default 49983). |
| Connect-Protocol-Version | header | connect-protocol-version | yes | const=1 | Define the version of the Connect protocol |
| Connect-Timeout-Ms | header | connect-timeout-header | no | — | Define the timeout, in ms |

#### Request body

Required: **yes**.

##### application/json

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `watcherId` | string | no | — | — |

#### Responses

| Status | Description | Content type | Schema |
|---|---|---|---|
| 200 | Success | application/json | filesystem.RemoveWatcherResponse |
| 502 | Sandbox not found | application/json | object |

#### Data structures

##### connect-protocol-version

Define the version of the Connect protocol

Type: `number`. Constraints: const=1. See the official contract linked above.

##### connect-timeout-header

Define the timeout, in ms

Type: `number`. Constraints: —. See the official contract linked above.

##### filesystem.RemoveWatcherRequest

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `watcherId` | string | no | — | — |

##### filesystem.RemoveWatcherResponse

Type: `object`. Constraints: —. See the official contract linked above.



### `POST /filesystem.Filesystem/WatchDir`



[Official contract](https://docs.e2b.dev/api-reference/filesystem/watchdir)

**Endpoint:** `POST /filesystem.Filesystem/WatchDir`

Servers: `https://sandbox.{domain}`.

Security (upstream OpenAPI): `[{"SandboxAccessTokenAuth": [], "SandboxUserAuth": []}]`. Scheme details are provided in the linked official contract.

#### Request parameters

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| Connect-Protocol-Version | header | connect-protocol-version | yes | const=1 | Define the version of the Connect protocol |
| Connect-Timeout-Ms | header | connect-timeout-header | no | — | Define the timeout, in ms |
| E2b-Sandbox-Id | header | string | yes | — | Identifier of the target sandbox. Routes the request to that sandbox's envd over the shared sandbox host. |
| E2b-Sandbox-Port | header | integer | yes | default=49983 | Port envd listens on inside the sandbox (default 49983). |

#### Request body

Required: **yes**.

##### application/connect+json

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `path` | string | no | — | — |
| `recursive` | boolean | no | — | — |
| `includeEntry` | boolean | no | — | If true, each FilesystemEvent includes the EntryInfo of the affected entry, when available. |
| `allowNetworkMounts` | boolean | no | — | If true, allows watching paths on network filesystem mounts (NFS, CIFS, SMB, FUSE).  Events on network mounts may be unreliable or not delivered at all. |

#### Responses

| Status | Description | Content type | Schema |
|---|---|---|---|
| 200 | Stream of WatchDirResponse events | application/connect+json | filesystem.WatchDirResponse |
| 502 | Sandbox not found | application/json | object |

#### Data structures

##### connect-protocol-version

Define the version of the Connect protocol

Type: `number`. Constraints: const=1. See the official contract linked above.

##### connect-timeout-header

Define the timeout, in ms

Type: `number`. Constraints: —. See the official contract linked above.

##### filesystem.WatchDirRequest

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `path` | string | no | — | — |
| `recursive` | boolean | no | — | — |
| `includeEntry` | boolean | no | — | If true, each FilesystemEvent includes the EntryInfo of the affected entry, when available. |
| `allowNetworkMounts` | boolean | no | — | If true, allows watching paths on network filesystem mounts (NFS, CIFS, SMB, FUSE).  Events on network mounts may be unreliable or not delivered at all. |

##### filesystem.WatchDirResponse

Type: `oneOf: see schema, see schema, see schema`. Constraints: —. See the official contract linked above.

##### filesystem.FilesystemEvent

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `name` | string | no | — | — |
| `type` | filesystem.EventType | no | enum=["EVENT_TYPE_UNSPECIFIED", "EVENT_TYPE_CREATE", "EVENT_TYPE_WRITE", "EVENT_TYPE_REMOVE", "EVENT_TYPE_RENAME", "EVENT_TYPE_CHMOD"] | — |
| `entry` | oneOf: filesystem.EntryInfo, null | no | — | Info of the entry that triggered the event. Only populated when include_entry  was requested and the entry could be stat-ed (e.g. not set for remove/rename-away  events, where the entry no longer exists at this path). |

##### filesystem.WatchDirResponse.KeepAlive

Type: `object`. Constraints: —. See the official contract linked above.

##### filesystem.WatchDirResponse.StartEvent

Type: `object`. Constraints: —. See the official contract linked above.

##### filesystem.EventType

Type: `string`. Constraints: enum=["EVENT_TYPE_UNSPECIFIED", "EVENT_TYPE_CREATE", "EVENT_TYPE_WRITE", "EVENT_TYPE_REMOVE", "EVENT_TYPE_RENAME", "EVENT_TYPE_CHMOD"]. See the official contract linked above.

##### filesystem.EntryInfo

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `name` | string | no | — | — |
| `type` | filesystem.FileType | no | enum=["FILE_TYPE_UNSPECIFIED", "FILE_TYPE_FILE", "FILE_TYPE_DIRECTORY", "FILE_TYPE_SYMLINK"] | — |
| `path` | string | no | — | — |
| `size` | integer / string | no | format="int64" | File size in bytes. Encoded as string for values exceeding JSON number precision (int64). |
| `mode` | integer | no | — | — |
| `permissions` | string | no | — | — |
| `owner` | string | no | — | — |
| `group` | string | no | — | — |
| `modifiedTime` | google.protobuf.Timestamp | no | format="date-time" | A Timestamp represents a point in time independent of any time zone or local  calendar, encoded as a count of seconds and fractions of seconds at  nanosecond resolution. The count is relative to an epoch at UTC midnight on  January 1, 1970, in the proleptic Gregorian calendar which extends the  Gregorian calendar backwards to year one.   All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap  second table is needed for interpretation, using a [24-hour linear  smear](https://developers.google.com/time/smear).   The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By  restricting to that range, we ensure that we can convert to and from [RFC  3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.   # Examples   Example 1: Compute Timestamp from POSIX `time()`.       Timestamp timestamp;      timestamp.set_seconds(time(NULL));      timestamp.set_nanos(0);   Example 2: Compute Timestamp from POSIX `gettimeofday()`.       struct timeval tv;      gettimeofday(&tv, NULL);       Timestamp timestamp;      timestamp.set_seconds(tv.tv_sec);      timestamp.set_nanos(tv.tv_usec * 1000);   Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.       FILETIME ft;      GetSystemTimeAsFileTime(&ft);      UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) \| ft.dwLowDateTime;       // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z      // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.      Timestamp timestamp;      timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));      timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));   Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.       long millis = System.currentTimeMillis();       Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)          .setNanos((int) ((millis % 1000) * 1000000)).build();   Example 5: Compute Timestamp from Java `Instant.now()`.       Instant now = Instant.now();       Timestamp timestamp =          Timestamp.newBuilder().setSeconds(now.getEpochSecond())              .setNanos(now.getNano()).build();   Example 6: Compute Timestamp from current time in Python.       timestamp = Timestamp()      timestamp.GetCurrentTime()   # JSON Mapping   In JSON format, the Timestamp type is encoded as a string in the  [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the  format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"  where {year} is always expressed using four digits while {month}, {day},  {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional  seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),  are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone  is required. A proto3 JSON serializer should always use UTC (as indicated by  "Z") when printing the Timestamp type and a proto3 JSON parser should be  able to accept both UTC and other timezones (as indicated by an offset).   For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past  01:30 UTC on January 15, 2017.   In JavaScript, one can convert a Date object to this format using the  standard  [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)  method. In Python, a standard `datetime.datetime` object can be converted  to this format using  [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with  the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use  the Joda Time's [`ISODateTimeFormat.dateTime()`](  http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()  ) to obtain a formatter capable of generating timestamps in this format. |
| `symlinkTarget` | string / null | no | — | If the entry is a symlink, this field contains the target of the symlink. |
| `metadata` | object | no | — | User-defined metadata stored as extended attributes (xattrs) on the file.  Keys live under the `user.e2b.` xattr namespace; the prefix is stripped here.  Plain `user.*` xattrs written by other tooling are not reflected. |

##### filesystem.FileType

Type: `string`. Constraints: enum=["FILE_TYPE_UNSPECIFIED", "FILE_TYPE_FILE", "FILE_TYPE_DIRECTORY", "FILE_TYPE_SYMLINK"]. See the official contract linked above.

##### google.protobuf.Timestamp

A Timestamp represents a point in time independent of any time zone or local
 calendar, encoded as a count of seconds and fractions of seconds at
 nanosecond resolution. The count is relative to an epoch at UTC midnight on
 January 1, 1970, in the proleptic Gregorian calendar which extends the
 Gregorian calendar backwards to year one.

 All minutes are 60 seconds long. Leap seconds are "smeared" so that no leap
 second table is needed for interpretation, using a [24-hour linear
 smear](https://developers.google.com/time/smear).

 The range is from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z. By
 restricting to that range, we ensure that we can convert to and from [RFC
 3339](https://www.ietf.org/rfc/rfc3339.txt) date strings.

 # Examples

 Example 1: Compute Timestamp from POSIX `time()`.

     Timestamp timestamp;
     timestamp.set_seconds(time(NULL));
     timestamp.set_nanos(0);

 Example 2: Compute Timestamp from POSIX `gettimeofday()`.

     struct timeval tv;
     gettimeofday(&tv, NULL);

     Timestamp timestamp;
     timestamp.set_seconds(tv.tv_sec);
     timestamp.set_nanos(tv.tv_usec * 1000);

 Example 3: Compute Timestamp from Win32 `GetSystemTimeAsFileTime()`.

     FILETIME ft;
     GetSystemTimeAsFileTime(&ft);
     UINT64 ticks = (((UINT64)ft.dwHighDateTime) << 32) | ft.dwLowDateTime;

     // A Windows tick is 100 nanoseconds. Windows epoch 1601-01-01T00:00:00Z
     // is 11644473600 seconds before Unix epoch 1970-01-01T00:00:00Z.
     Timestamp timestamp;
     timestamp.set_seconds((INT64) ((ticks / 10000000) - 11644473600LL));
     timestamp.set_nanos((INT32) ((ticks % 10000000) * 100));

 Example 4: Compute Timestamp from Java `System.currentTimeMillis()`.

     long millis = System.currentTimeMillis();

     Timestamp timestamp = Timestamp.newBuilder().setSeconds(millis / 1000)
         .setNanos((int) ((millis % 1000) * 1000000)).build();

 Example 5: Compute Timestamp from Java `Instant.now()`.

     Instant now = Instant.now();

     Timestamp timestamp =
         Timestamp.newBuilder().setSeconds(now.getEpochSecond())
             .setNanos(now.getNano()).build();

 Example 6: Compute Timestamp from current time in Python.

     timestamp = Timestamp()
     timestamp.GetCurrentTime()

 # JSON Mapping

 In JSON format, the Timestamp type is encoded as a string in the
 [RFC 3339](https://www.ietf.org/rfc/rfc3339.txt) format. That is, the
 format is "{year}-{month}-{day}T{hour}:{min}:{sec}[.{frac_sec}]Z"
 where {year} is always expressed using four digits while {month}, {day},
 {hour}, {min}, and {sec} are zero-padded to two digits each. The fractional
 seconds, which can go up to 9 digits (i.e. up to 1 nanosecond resolution),
 are optional. The "Z" suffix indicates the timezone ("UTC"); the timezone
 is required. A proto3 JSON serializer should always use UTC (as indicated by
 "Z") when printing the Timestamp type and a proto3 JSON parser should be
 able to accept both UTC and other timezones (as indicated by an offset).

 For example, "2017-01-15T01:30:15.01Z" encodes 15.01 seconds past
 01:30 UTC on January 15, 2017.

 In JavaScript, one can convert a Date object to this format using the
 standard
 [toISOString()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date/toISOString)
 method. In Python, a standard `datetime.datetime` object can be converted
 to this format using
 [`strftime`](https://docs.python.org/2/library/time.html#time.strftime) with
 the time format spec '%Y-%m-%dT%H:%M:%S.%fZ'. Likewise, in Java, one can use
 the Joda Time's [`ISODateTimeFormat.dateTime()`](
 http://joda-time.sourceforge.net/apidocs/org/joda/time/format/ISODateTimeFormat.html#dateTime()
 ) to obtain a formatter capable of generating timestamps in this format.

Type: `string`. Constraints: format="date-time". See the official contract linked above.

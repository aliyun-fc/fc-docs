# E2B SDK-compatible API List

This topic describes the main data-plane capability boundaries that FC Agent Sandbox provides through E2B SDK and CLI compatibility. The "APIs" in this topic are E2B SDK object methods and E2B CLI commands, not native FC Agent Sandbox control-plane OpenAPI operations. FC Agent Sandbox does not provide a standalone data-plane SDK. Resource management capabilities such as Team, API Key, and Quota are available through native OpenAPI, Alibaba Cloud SDK, or Alibaba Cloud CLI. For details, see [Alibaba Cloud SDK and API Guide](04.Alibaba%20Cloud%20SDK%20and%20API%20Guide.md).

Keep the following three points in mind when reading this topic:

- Method names and parameter shapes depend on the E2B SDK version you are using. Python and TypeScript may follow different naming styles.
- Capabilities marked as compatible still need to be validated against account quotas, region availability, template status, and SDK version.
- Capabilities marked as limited or unsupported should not be treated as the primary production integration path.

## Sandbox

`Sandbox` is the core object in FC Agent Sandbox. The current primary path is to use the E2B SDK to create, connect to, inspect, pause, resume, set timeouts for, and terminate sandboxes.

Compatible capabilities:

- Create a sandbox: `Sandbox.create()`.
- Connect to an existing sandbox: `Sandbox.connect(sandboxId)`.
- List sandboxes: `Sandbox.list()`.
- Query sandbox information: `Sandbox.getInfo(sandboxId)`, `sandbox.getInfo()`.
- Check runtime status: `sandbox.isRunning()`.
- Set timeouts: `Sandbox.setTimeout(sandboxId, ms)`, `sandbox.setTimeout(ms)`.
- Pause a sandbox (requires allowlist access): `sandbox.pause()`.
- Terminate a sandbox: `Sandbox.kill(sandboxId)`, `sandbox.kill()`.
- Get file upload and download URLs: `sandbox.uploadUrl(path)`, `sandbox.downloadUrl(path)`.
- Get a port access address: `sandbox.getHost(port)`.
- Create a Snapshot: `sandbox.createSnapshot({ name? })` / `Sandbox.createSnapshot(sandboxId, { name? })`.
- List Snapshots: `Sandbox.listSnapshots({ sandboxId?, name?, limit?, nextToken? })`.
- Delete a Snapshot: `Sandbox.deleteSnapshot(snapshotId | qualifiedName)`.

Usage recommendations:

- Business code should release sandboxes in `try/finally` blocks to avoid leaving resources allocated after task failures.
- `Sandbox.connect(sandboxId)` is suitable for resuming or taking over an existing sandbox. The connection fails if the target sandbox has already been terminated, reclaimed due to timeout, or belongs to a different account or region.
- Port access is suitable for previewing temporary web services. It should not directly replace a formal API gateway, authentication, or traffic governance.
- The template argument of `Sandbox.create()` can also be a Snapshot ID or the qualified name of a named Snapshot. Snapshot requires allowlist access and the generation-2 runtime (`micro-sandbox`). For Snapshot limits, see [Snapshots](../04.Features/01.Sandbox/12.Snapshots.md).

## Commands

`Commands` is used to run commands inside a sandbox, manage background processes, and handle input and output.

Compatible capabilities:

- Run a command: `sandbox.commands.run()`.
- List commands or processes: `sandbox.commands.list()`.
- Connect to a background command: `sandbox.commands.connect()`.
- Send stdin to a command: `sandbox.commands.sendStdin()`.
- Kill a command: `sandbox.commands.kill()`.
- Interactive terminals: use the independent `sandbox.pty` API.

Usage recommendations:

- Use `commands.run()` for standard batch commands whenever possible. Use `sandbox.pty` only for interactive CLIs, colored output, progress bars, or commands that depend on TTY detection.
- For long-running tasks, set command-level timeouts and process stdout and stderr instead of waiting only for the final result.
- Do not concatenate unvalidated user input directly into shell commands.

## Filesystem

`Filesystem` is used to read and write files and manage directories during the sandbox lifecycle.

Compatible capabilities:

- List a directory: `sandbox.files.list()`.
- Check whether a path exists: `sandbox.files.exists()`.
- Get file or directory metadata: `sandbox.files.getInfo()` / `sandbox.files.get_info()`.
- Read a file: `sandbox.files.read()`.
- Write a file: `sandbox.files.write()`.
- Create a directory: `sandbox.files.makeDir()`.
- Delete a file or directory: `sandbox.files.remove()`.
- Rename a file or directory: `sandbox.files.rename()`.
- Watch a directory: `sandbox.files.watchDir()` / `sandbox.files.watch_dir()`.

Usage recommendations:

- FC Agent Sandbox does not currently support custom file metadata. Even when the SDK's `files.write()` signature accepts `metadata`, the call can fail before the request is sent because the sandbox runs an `envd` version earlier than `0.6.2`. Regular file reads and writes are unaffected.
- Directory watching is useful for waiting for file generation or syncing task artifacts. If you only need to wait for a single file, bounded polling with `exists()` also works.
- The local sandbox filesystem is suitable for temporary task files, not for long-term persistent storage.
- Data that must be retained, shared, or preserved across sandboxes should be written to external storage such as NAS or OSS.
- When handling user-uploaded paths, restrict writable directories and file sizes to avoid path traversal and resource exhaustion.

## Code Interpreter

`Code Interpreter` is used to execute code snippets and preserve context across multiple executions.

Compatible capabilities:

- Run code: `sandbox.runCode()` / `sandbox.run_code()`.
- Create an execution context: `sandbox.createCodeContext()` / `sandbox.create_code_context()`.
- Query execution contexts: `sandbox.listCodeContexts()` / `sandbox.list_code_contexts()`.
- Restart execution contexts: `sandbox.restartCodeContext()` / `sandbox.restart_code_context()`.
- Delete execution contexts: `sandbox.removeCodeContext()` / `sandbox.remove_code_context()`.
- Retrieve execution output, including stdout, stderr, execution count, expression results, and rich results in `execution.results`.
- Receive stdout, stderr, and result callbacks.
- Run Python, JavaScript, TypeScript, and Bash through the `language` parameter. Java and R are not supported.
- Preserve variables and execution state within the same context.

Usage recommendations:

- Code Interpreter is suitable for data analysis, code interpreter, and notebook-style tasks.
- If the task is closer to fixed-script execution, `Commands` is easier for auditing, timeout control, and output validation.
- Do not allow code to access arbitrary paths, arbitrary networks, or unlimited resources by default.

## Templates

`Templates` are used to define reusable runtime environments. The current primary path is to reuse built-in templates or build custom templates, and then create sandboxes through the SDK or CLI.

Compatible capabilities:

- Query templates.
- Create, update, or delete templates.
- Build templates.
- Manage templates by tags, versions, or aliases.
- Create sandboxes from templates.

Usage recommendations:

- In production, pin the template name, version, or alias. Avoid uncontrolled changes caused by references such as `latest`.
- Preinstall language runtimes, system dependencies, base CLIs, and stable business SDKs in the template to reduce per-sandbox runtime installation.
- Template builds depend on image registries, networks, and account permissions. If a build fails, check those prerequisites before troubleshooting SDK parameters.

## CLI

FC Agent Sandbox is compatible with the commonly used E2B CLI commands for debugging and management. The CLI is suitable for local validation, troubleshooting, and lightweight operations. It should not be used as the primary control plane for production systems.

Compatible commands:

- View templates: `e2b template list`.
- Create a sandbox: `e2b sandbox create`.
- View sandboxes: `e2b sandbox list`.
- Terminate a sandbox: `e2b sandbox kill <sandbox-id>`.
- Connect to a sandbox: `e2b sandbox connect <sandbox-id>`.
- Execute a command: `e2b sandbox exec <sandbox-id> <command>`.
- View metrics: `e2b sandbox metrics <sandbox-id>`.

Configuration requirements:

```bash
export E2B_API_KEY="<your-api-key>"
export E2B_API_URL="https://api.<region>.e2b.fc.aliyuncs.com"
export E2B_DOMAIN="<region>.e2b.fc.aliyuncs.com"
```

`E2B_ACCESS_TOKEN` is a deprecated legacy E2B authentication variable. New E2B CLI versions should use `E2B_API_KEY`. CLI parameters may change between versions. If command behavior does not match the documentation, first run `e2b --version` and the target command with `--help` to confirm the installed CLI version and parameter shape.

## Limited capabilities

The following E2B SDK capabilities currently exist only to preserve invocation compatibility or return placeholder results, and they should not be used as production dependencies:

- Sandbox Logs: may return empty results. Do not use for formal log collection.
- Network Config Update: supports replacement of outbound rules and transforms on running sandboxes, subject to field restrictions.

Sandbox Metrics currently supports viewing CPU and memory metrics. Disk and page-cache fields return placeholder values and should not be used for capacity decisions. Metrics return at 1-minute granularity. Use the Function Compute console, CloudMonitor, or Simple Log Service as the source of truth for billing, alerting, and capacity governance.

If your workload requires logs, network changes, VPC connectivity, or access to cloud storage, design those capabilities separately according to the currently available FC Agent Sandbox capabilities and Alibaba Cloud infrastructure options. Do not copy the E2B API design directly.

## Capabilities not currently supported

The following capabilities are not currently part of the FC Agent Sandbox E2B compatibility path:

- Volume API.
- Team management (manage Teams in the Function Compute console).
- API key and access token management.
- E2B-hosted MCP Gateway.
- E2B custom domains, proxy tunnels, Bring Your Own Cloud, and other platform-side capabilities.

Snapshots are supported. For details, see [Snapshots](../04.Features/01.Sandbox/12.Snapshots.md).

When migrating an existing E2B application, first identify whether it depends on any of these capabilities. If it does, adjust the architecture or confirm whether FC Agent Sandbox provides an alternative before starting the integration.

## HTTP request and response fields

The following protocol details were checked against the official E2B OpenAPI on 2026-09-08. Required properties apply to their containing object. FC support and differences follow the guidance on this page and the [compatibility inventory](01.E2B%20SDK-compatible%20API%20List.md). Upstream defaults, status codes and new fields do not establish FC behavior.

### `GET /health`

This section documents the upstream contract; check the preceding compatibility inventory before using it on FC.

[Official contract](https://docs.e2b.dev/api-reference/envd/check-the-health-of-the-service)

**Endpoint:** `GET /health`

Servers: `https://sandbox.{domain}`.

Security (upstream OpenAPI): `[]`. Scheme details are provided in the linked official contract.

#### Request parameters

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| E2b-Sandbox-Id | header | string | yes | — | Identifier of the target sandbox. Routes the request to that sandbox's envd over the shared sandbox host. |
| E2b-Sandbox-Port | header | integer | yes | default=49983 | Port envd listens on inside the sandbox (default 49983). |

#### Responses

| Status | Description | Content type | Schema |
|---|---|---|---|
| 204 | The service is healthy | none declared | — |
| 502 | Sandbox not found | application/json | object |



### `GET /metrics`

This section documents the upstream contract; check the preceding compatibility inventory before using it on FC.

[Official contract](https://docs.e2b.dev/api-reference/envd/service-stats)

**Endpoint:** `GET /metrics`

Servers: `https://sandbox.{domain}`.

Security (upstream OpenAPI): `[{"SandboxAccessTokenAuth": []}]`. Scheme details are provided in the linked official contract.

#### Request parameters

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| E2b-Sandbox-Id | header | string | yes | — | Identifier of the target sandbox. Routes the request to that sandbox's envd over the shared sandbox host. |
| E2b-Sandbox-Port | header | integer | yes | default=49983 | Port envd listens on inside the sandbox (default 49983). |

#### Responses

| Status | Description | Content type | Schema |
|---|---|---|---|
| 200 | The resource usage metrics of the service | application/json | Metrics |
| 502 | Sandbox not found | application/json | object |

#### Data structures

##### Metrics

Resource usage metrics

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `ts` | integer | no | format="int64" | Unix timestamp in UTC for current sandbox time |
| `cpu_count` | integer | no | — | Number of CPU cores |
| `cpu_used_pct` | number | no | format="float" | CPU usage percentage |
| `mem_total` | integer | no | format="int64" | Total virtual memory in bytes |
| `mem_used` | integer | no | format="int64" | Used virtual memory in bytes |
| `mem_cache` | integer | no | — | Cached memory (page cache) in bytes |
| `mem_total_mib` | integer | no | format="int64" | Total virtual memory in MiB |
| `mem_used_mib` | integer | no | format="int64" | Used virtual memory in MiB |
| `disk_used` | integer | no | format="int64" | Used disk space in bytes |
| `disk_total` | integer | no | format="int64" | Total disk space in bytes |



### `POST /sandboxes/{sandboxID}/snapshots`

This section documents the upstream contract; check the preceding compatibility inventory before using it on FC.

[Official contract](https://docs.e2b.dev/api-reference/sandboxes/create-snapshot)

**Endpoint:** `POST /sandboxes/{sandboxID}/snapshots`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### Request parameters

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| sandboxID | path | string | yes | — | — |

#### Request body

Required: **yes**.

##### application/json

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `name` | string | no | — | Optional name for the snapshot template. If a snapshot template with this name already exists, a new build will be assigned to the existing template instead of creating a new one. |

#### Responses

| Status | Description | Content type | Schema |
|---|---|---|---|
| 201 | Snapshot created successfully | application/json | SnapshotInfo |
| 400 | Bad request | application/json | Error |
| 401 | Authentication error | application/json | Error |
| 404 | Not found | application/json | Error |
| 500 | Server error | application/json | Error |

#### Data structures

##### SandboxSnapshotRequest

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `name` | string | no | — | Optional name for the snapshot template. If a snapshot template with this name already exists, a new build will be assigned to the existing template instead of creating a new one. |

##### SnapshotInfo

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `snapshotID` | string | yes | — | Identifier of the snapshot template including the tag. Uses namespace/alias when a name was provided (e.g. team-slug/my-snapshot:default), otherwise falls back to the raw template ID (e.g. abc123:default). |
| `names` | array of string | yes | — | Full names of the snapshot template including team namespace and tag (e.g. team-slug/my-snapshot:v2) |

##### Error

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |



### `POST /sandboxes/{sandboxID}/fork`

This section documents the upstream contract; check the preceding compatibility inventory before using it on FC.

[Official contract](https://docs.e2b.dev/api-reference/sandboxes/fork-sandbox)

**Endpoint:** `POST /sandboxes/{sandboxID}/fork`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### Request parameters

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| sandboxID | path | string | yes | — | — |

#### Request body

Required: **no**.

##### application/json

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `timeout` | integer | no | default=15; minimum=0; format="int32" | Time to live for the new forked sandboxes in seconds. |
| `count` | integer | no | default=1; minimum=1; maximum=100; format="int32" | Number of forked sandboxes to create. All forks boot from the same snapshot, so the snapshot is captured once regardless of count. Each fork succeeds or fails independently; the outcome of each is reported in its entry of the response list. |

#### Responses

| Status | Description | Content type | Schema |
|---|---|---|---|
| 201 | The sandbox was snapshotted and the forks were attempted; each entry reports one fork's outcome | application/json | array of SandboxForkResult |
| 401 | Authentication error | application/json | Error |
| 404 | Not found | application/json | Error |
| 409 | Conflict | application/json | Error |
| 500 | Server error | application/json | Error |
| 503 | Service unavailable | application/json | Error |

#### Data structures

##### SandboxForkRequest

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `timeout` | integer | no | default=15; minimum=0; format="int32" | Time to live for the new forked sandboxes in seconds. |
| `count` | integer | no | default=1; minimum=1; maximum=100; format="int32" | Number of forked sandboxes to create. All forks boot from the same snapshot, so the snapshot is captured once regardless of count. Each fork succeeds or fails independently; the outcome of each is reported in its entry of the response list. |

##### SandboxForkResult

Result of one requested fork. Exactly one of sandbox or error is set: sandbox when the fork started successfully, error when it failed to start.

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `sandbox` | Sandbox | no | — | — |
| `error` | Error | no | — | — |

##### Sandbox

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `templateID` | string | yes | — | Identifier of the template from which is the sandbox created |
| `sandboxID` | string | yes | — | Identifier of the sandbox |
| `alias` | string | no | — | Alias of the template |
| `clientID` | string | yes | deprecated=true | Identifier of the client |
| `envdVersion` | EnvdVersion | yes | — | Version of the envd running in the sandbox |
| `envdAccessToken` | string / null | no | — | Access token for authenticating envd requests to this sandbox. Only returned when the sandbox is created with `secure: true`. Null for non-secure sandboxes (envd endpoints work without auth). |
| `trafficAccessToken` | string / null | no | — | Token required for accessing sandbox via proxy. |
| `domain` | string / null | no | deprecated=true | Deprecated: always null. Construct sandbox URLs as `https://{port}-{sandboxID}.e2b.app`. |

##### Error

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |

##### EnvdVersion

Version of the envd running in the sandbox

Type: `string`. Constraints: —. See the official contract linked above.



### `GET /snapshots`

This section documents the upstream contract; check the preceding compatibility inventory before using it on FC.

[Official contract](https://docs.e2b.dev/api-reference/sandboxes/list-snapshots)

**Endpoint:** `GET /snapshots`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### Request parameters

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| sandboxID | query | string | no | — | Filter snapshots by source sandbox ID |
| name | query | string | no | — | Filter snapshots by name or ID, optionally tag-qualified (e.g. "my-snapshot", "my-team/my-snapshot" or "my-snapshot:v1"). |
| limit | query | integer | no | default=100; minimum=1; maximum=100; format="int32" | Maximum number of items to return per page |
| nextToken | query | string | no | — | Cursor to start the list from |

#### Responses

| Status | Description | Content type | Schema |
|---|---|---|---|
| 200 | Successfully returned snapshots | application/json | array of SnapshotInfo |
| 401 | Authentication error | application/json | Error |
| 500 | Server error | application/json | Error |

#### Data structures

##### SnapshotInfo

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `snapshotID` | string | yes | — | Identifier of the snapshot template including the tag. Uses namespace/alias when a name was provided (e.g. team-slug/my-snapshot:default), otherwise falls back to the raw template ID (e.g. abc123:default). |
| `names` | array of string | yes | — | Full names of the snapshot template including team namespace and tag (e.g. team-slug/my-snapshot:v2) |

##### Error

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |



### `POST /secrets`

This section documents the upstream contract; check the preceding compatibility inventory before using it on FC.

[Official contract](https://docs.e2b.dev/api-reference/secrets/create-a-secret)

**Endpoint:** `POST /secrets`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### Request body

Required: **yes**.

##### application/json

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `name` | string | yes | minLength=1; maxLength=128; pattern="^[a-zA-Z0-9_-]+$" | Name of the secret, unique within the project. Names are lower-cased before storage and returned in that canonical form; the sec_ prefix is reserved for secret identifiers.  |
| `value` | string | yes | — | Runtime marker stored as the secret's first version. The runtime resolves it to a value at sandbox egress. |
| `metadata` | SecretMetadata | no | — | Customer metadata of the secret. Always present, empty when unset. At most 32 entries; keys are limited to 128 bytes, values to 1024 bytes, and a secret's metadata to 8192 bytes in total.  |

#### Responses

| Status | Description | Content type | Schema |
|---|---|---|---|
| 201 | Successfully created the secret | application/json | Secret |
| 400 | Bad request | application/json | Error |
| 401 | Authentication error | application/json | Error |
| 403 | Forbidden | application/json | Error |
| 404 | Not found | application/json | Error |
| 409 | Conflict | application/json | Error |
| 429 | Too many requests | application/json | Error |
| 500 | Server error | application/json | Error |
| 502 | Backend error | application/json | Error |
| 504 | Backend timeout | application/json | Error |

#### Data structures

##### NewSecret

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `name` | string | yes | minLength=1; maxLength=128; pattern="^[a-zA-Z0-9_-]+$" | Name of the secret, unique within the project. Names are lower-cased before storage and returned in that canonical form; the sec_ prefix is reserved for secret identifiers.  |
| `value` | string | yes | — | Runtime marker stored as the secret's first version. The runtime resolves it to a value at sandbox egress. |
| `metadata` | SecretMetadata | no | — | Customer metadata of the secret. Always present, empty when unset. At most 32 entries; keys are limited to 128 bytes, values to 1024 bytes, and a secret's metadata to 8192 bytes in total.  |

##### Secret

Metadata of a secret. It never carries the secret value.

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `secretID` | string | yes | — | Identifier of the secret |
| `name` | string | yes | — | Name of the secret, unique within the project |
| `currentVersion` | integer | yes | format="int64" | Version served to readers that do not name one |
| `metadata` | SecretMetadata | yes | — | Customer metadata of the secret. Always present, empty when unset. At most 32 entries; keys are limited to 128 bytes, values to 1024 bytes, and a secret's metadata to 8192 bytes in total.  |
| `createdAt` | string | yes | format="date-time" | Time when the secret was created |
| `updatedAt` | string | yes | format="date-time" | Time when the secret was last updated |

##### SecretMetadata

Customer metadata of the secret. Always present, empty when unset. At most 32 entries; keys are limited to 128 bytes, values to 1024 bytes, and a secret's metadata to 8192 bytes in total.


Type: `object`. Constraints: —. See the official contract linked above.

##### Error

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |



### `DELETE /secrets/{secretID}`

This section documents the upstream contract; check the preceding compatibility inventory before using it on FC.

[Official contract](https://docs.e2b.dev/api-reference/secrets/delete-a-secret)

**Endpoint:** `DELETE /secrets/{secretID}`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### Request parameters

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| secretID | path | string | yes | — | Identifier of the secret (sec_ prefixed), or its canonical lower-case name  |

#### Responses

| Status | Description | Content type | Schema |
|---|---|---|---|
| 204 | Successfully deleted the secret | none declared | — |
| 400 | Bad request | application/json | Error |
| 401 | Authentication error | application/json | Error |
| 403 | Forbidden | application/json | Error |
| 404 | Not found | application/json | Error |
| 409 | Conflict | application/json | Error |
| 429 | Too many requests | application/json | Error |
| 500 | Server error | application/json | Error |
| 502 | Backend error | application/json | Error |
| 504 | Backend timeout | application/json | Error |

#### Data structures

##### Error

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |



### `GET /secrets/{secretID}`

This section documents the upstream contract; check the preceding compatibility inventory before using it on FC.

[Official contract](https://docs.e2b.dev/api-reference/secrets/get-a-secret)

**Endpoint:** `GET /secrets/{secretID}`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### Request parameters

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| secretID | path | string | yes | — | Identifier of the secret (sec_ prefixed), or its canonical lower-case name  |

#### Responses

| Status | Description | Content type | Schema |
|---|---|---|---|
| 200 | Successfully retrieved the secret | application/json | Secret |
| 400 | Bad request | application/json | Error |
| 401 | Authentication error | application/json | Error |
| 403 | Forbidden | application/json | Error |
| 404 | Not found | application/json | Error |
| 409 | Conflict | application/json | Error |
| 429 | Too many requests | application/json | Error |
| 500 | Server error | application/json | Error |
| 502 | Backend error | application/json | Error |
| 504 | Backend timeout | application/json | Error |

#### Data structures

##### Secret

Metadata of a secret. It never carries the secret value.

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `secretID` | string | yes | — | Identifier of the secret |
| `name` | string | yes | — | Name of the secret, unique within the project |
| `currentVersion` | integer | yes | format="int64" | Version served to readers that do not name one |
| `metadata` | SecretMetadata | yes | — | Customer metadata of the secret. Always present, empty when unset. At most 32 entries; keys are limited to 128 bytes, values to 1024 bytes, and a secret's metadata to 8192 bytes in total.  |
| `createdAt` | string | yes | format="date-time" | Time when the secret was created |
| `updatedAt` | string | yes | format="date-time" | Time when the secret was last updated |

##### SecretMetadata

Customer metadata of the secret. Always present, empty when unset. At most 32 entries; keys are limited to 128 bytes, values to 1024 bytes, and a secret's metadata to 8192 bytes in total.


Type: `object`. Constraints: —. See the official contract linked above.

##### Error

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |



### `GET /secrets`

This section documents the upstream contract; check the preceding compatibility inventory before using it on FC.

[Official contract](https://docs.e2b.dev/api-reference/secrets/list-project-secrets)

**Endpoint:** `GET /secrets`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### Request parameters

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| nextToken | query | string | no | — | Cursor to start the list from |
| limit | query | integer | no | default=100; minimum=1; maximum=100; format="int32" | Maximum number of items to return per page |

#### Responses

| Status | Description | Content type | Schema |
|---|---|---|---|
| 200 | Successfully listed the project's secrets | application/json | array of Secret |
| 400 | Bad request | application/json | Error |
| 401 | Authentication error | application/json | Error |
| 403 | Forbidden | application/json | Error |
| 404 | Not found | application/json | Error |
| 409 | Conflict | application/json | Error |
| 429 | Too many requests | application/json | Error |
| 500 | Server error | application/json | Error |
| 502 | Backend error | application/json | Error |
| 504 | Backend timeout | application/json | Error |

#### Data structures

##### Secret

Metadata of a secret. It never carries the secret value.

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `secretID` | string | yes | — | Identifier of the secret |
| `name` | string | yes | — | Name of the secret, unique within the project |
| `currentVersion` | integer | yes | format="int64" | Version served to readers that do not name one |
| `metadata` | SecretMetadata | yes | — | Customer metadata of the secret. Always present, empty when unset. At most 32 entries; keys are limited to 128 bytes, values to 1024 bytes, and a secret's metadata to 8192 bytes in total.  |
| `createdAt` | string | yes | format="date-time" | Time when the secret was created |
| `updatedAt` | string | yes | format="date-time" | Time when the secret was last updated |

##### SecretMetadata

Customer metadata of the secret. Always present, empty when unset. At most 32 entries; keys are limited to 128 bytes, values to 1024 bytes, and a secret's metadata to 8192 bytes in total.


Type: `object`. Constraints: —. See the official contract linked above.

##### Error

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |



### `POST /secrets/{secretID}`

This section documents the upstream contract; check the preceding compatibility inventory before using it on FC.

[Official contract](https://docs.e2b.dev/api-reference/secrets/update-a-secret)

**Endpoint:** `POST /secrets/{secretID}`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### Request parameters

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| secretID | path | string | yes | — | Identifier of the secret (sec_ prefixed), or its canonical lower-case name  |

#### Request body

Required: **yes**.

##### application/json

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `value` | string | yes | — | Runtime marker stored as the secret's new version. The runtime resolves it to a value at sandbox egress. |
| `metadata` | SecretMetadata | no | — | Customer metadata of the secret. Always present, empty when unset. At most 32 entries; keys are limited to 128 bytes, values to 1024 bytes, and a secret's metadata to 8192 bytes in total.  |

#### Responses

| Status | Description | Content type | Schema |
|---|---|---|---|
| 200 | Successfully updated the secret | application/json | Secret |
| 400 | Bad request | application/json | Error |
| 401 | Authentication error | application/json | Error |
| 403 | Forbidden | application/json | Error |
| 404 | Not found | application/json | Error |
| 409 | Conflict | application/json | Error |
| 429 | Too many requests | application/json | Error |
| 500 | Server error | application/json | Error |
| 502 | Backend error | application/json | Error |
| 504 | Backend timeout | application/json | Error |

#### Data structures

##### SecretUpdate

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `value` | string | yes | — | Runtime marker stored as the secret's new version. The runtime resolves it to a value at sandbox egress. |
| `metadata` | SecretMetadata | no | — | Customer metadata of the secret. Always present, empty when unset. At most 32 entries; keys are limited to 128 bytes, values to 1024 bytes, and a secret's metadata to 8192 bytes in total.  |

##### Secret

Metadata of a secret. It never carries the secret value.

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `secretID` | string | yes | — | Identifier of the secret |
| `name` | string | yes | — | Name of the secret, unique within the project |
| `currentVersion` | integer | yes | format="int64" | Version served to readers that do not name one |
| `metadata` | SecretMetadata | yes | — | Customer metadata of the secret. Always present, empty when unset. At most 32 entries; keys are limited to 128 bytes, values to 1024 bytes, and a secret's metadata to 8192 bytes in total.  |
| `createdAt` | string | yes | format="date-time" | Time when the secret was created |
| `updatedAt` | string | yes | format="date-time" | Time when the secret was last updated |

##### SecretMetadata

Customer metadata of the secret. Always present, empty when unset. At most 32 entries; keys are limited to 128 bytes, values to 1024 bytes, and a secret's metadata to 8192 bytes in total.


Type: `object`. Constraints: —. See the official contract linked above.

##### Error

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |



### `GET /teams/{teamID}/metrics/max`

This section documents the upstream contract; check the preceding compatibility inventory before using it on FC.

[Official contract](https://docs.e2b.dev/api-reference/teams/get-team-metrics-max)

**Endpoint:** `GET /teams/{teamID}/metrics/max`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### Request parameters

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| teamID | path | string | yes | — | — |
| start | query | integer | no | minimum=0; format="int64" | Unix timestamp for the start of the interval, in seconds, for which the metrics are returned. |
| end | query | integer | no | minimum=0; format="int64" | Unix timestamp for the end of the interval, in seconds, for which the metrics are returned. |
| metric | query | string | yes | enum=["concurrent_sandboxes", "sandbox_start_rate"] | Metric to retrieve the maximum value for |

#### Responses

| Status | Description | Content type | Schema |
|---|---|---|---|
| 200 | Successfully returned the team metrics | application/json | MaxTeamMetric |
| 400 | Bad request | application/json | Error |
| 401 | Authentication error | application/json | Error |
| 403 | Forbidden | application/json | Error |
| 500 | Server error | application/json | Error |

#### Data structures

##### MaxTeamMetric

Team metric with timestamp

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `timestamp` | string | yes | deprecated=true; format="date-time" | Timestamp of the metric entry |
| `timestampUnix` | integer | yes | format="int64" | Timestamp of the metric entry in Unix time (seconds since epoch) |
| `value` | number | yes | — | The maximum value of the requested metric in the given interval |

##### Error

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |



### `GET /teams/{teamID}/metrics`

This section documents the upstream contract; check the preceding compatibility inventory before using it on FC.

[Official contract](https://docs.e2b.dev/api-reference/teams/get-team-metrics)

**Endpoint:** `GET /teams/{teamID}/metrics`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### Request parameters

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| teamID | path | string | yes | — | — |
| start | query | integer | no | minimum=0; format="int64" | Unix timestamp for the start of the interval, in seconds, for which the metrics are returned. |
| end | query | integer | no | minimum=0; format="int64" | Unix timestamp for the end of the interval, in seconds, for which the metrics are returned. |

#### Responses

| Status | Description | Content type | Schema |
|---|---|---|---|
| 200 | Successfully returned the team metrics | application/json | array of TeamMetric |
| 400 | Bad request | application/json | Error |
| 401 | Authentication error | application/json | Error |
| 403 | Forbidden | application/json | Error |
| 500 | Server error | application/json | Error |

#### Data structures

##### TeamMetric

Team metric with timestamp

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `timestamp` | string | yes | deprecated=true; format="date-time" | Timestamp of the metric entry |
| `timestampUnix` | integer | yes | format="int64" | Timestamp of the metric entry in Unix time (seconds since epoch) |
| `concurrentSandboxes` | integer | yes | format="int32" | The number of concurrent sandboxes for the team |
| `sandboxStartRate` | number | yes | format="float" | Number of sandboxes started per second |

##### Error

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |



### `GET /teams`

This section documents the upstream contract; check the preceding compatibility inventory before using it on FC.

[Official contract](https://docs.e2b.dev/api-reference/teams/list-teams)

**Endpoint:** `GET /teams`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[]`. Scheme details are provided in the linked official contract.

#### Responses

| Status | Description | Content type | Schema |
|---|---|---|---|
| 200 | Successfully returned all teams | application/json | array of Team |
| 401 | Authentication error | application/json | Error |
| 500 | Server error | application/json | Error |

#### Data structures

##### Team

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `teamID` | string | yes | — | Identifier of the team |
| `name` | string | yes | — | Name of the team |
| `apiKey` | string | yes | — | API key for the team |
| `isDefault` | boolean | yes | — | Whether the team is the default team |

##### Error

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |



### `POST /volumes`

This section documents the upstream contract; check the preceding compatibility inventory before using it on FC.

[Official contract](https://docs.e2b.dev/api-reference/volumes/create-team-volume)

**Endpoint:** `POST /volumes`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### Request body

Required: **yes**.

##### application/json

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `name` | string | yes | pattern="^[a-zA-Z0-9_-]+$" | Name of the volume |

#### Responses

| Status | Description | Content type | Schema |
|---|---|---|---|
| 201 | Successfully created a new team volume | application/json | VolumeAndToken |
| 400 | Bad request | application/json | Error |
| 401 | Authentication error | application/json | Error |
| 500 | Server error | application/json | Error |

#### Data structures

##### NewVolume

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `name` | string | yes | pattern="^[a-zA-Z0-9_-]+$" | Name of the volume |

##### VolumeAndToken

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `volumeID` | string | yes | — | ID of the volume |
| `name` | string | yes | — | Name of the volume |
| `token` | string | yes | — | Auth token to use for interacting with volume content |
| `domain` | string | no | — | Domain to use as the destination for volume content requests, replacing the default `api.<E2B_DOMAIN>`. Only returned when the team is connected to a custom (BYOC) cluster; absent otherwise, in which case the default domain is used.  |

##### Error

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |



### `DELETE /volumes/{volumeID}`

This section documents the upstream contract; check the preceding compatibility inventory before using it on FC.

[Official contract](https://docs.e2b.dev/api-reference/volumes/delete-team-volume)

**Endpoint:** `DELETE /volumes/{volumeID}`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### Request parameters

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| volumeID | path | string | yes | — | — |

#### Responses

| Status | Description | Content type | Schema |
|---|---|---|---|
| 204 | Successfully deleted a team volume | none declared | — |
| 401 | Authentication error | application/json | Error |
| 404 | Not found | application/json | Error |
| 500 | Server error | application/json | Error |

#### Data structures

##### Error

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |



### `GET /volumes`

This section documents the upstream contract; check the preceding compatibility inventory before using it on FC.

[Official contract](https://docs.e2b.dev/api-reference/volumes/list-team-volumes)

**Endpoint:** `GET /volumes`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### Responses

| Status | Description | Content type | Schema |
|---|---|---|---|
| 200 | Successfully listed all team volumes | application/json | array of Volume |
| 401 | Authentication error | application/json | Error |
| 500 | Server error | application/json | Error |

#### Data structures

##### Volume

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `volumeID` | string | yes | — | ID of the volume |
| `name` | string | yes | — | Name of the volume |

##### Error

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |



### `GET /volumes/{volumeID}`

This section documents the upstream contract; check the preceding compatibility inventory before using it on FC.

[Official contract](https://docs.e2b.dev/api-reference/volumes/team-volume)

**Endpoint:** `GET /volumes/{volumeID}`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### Request parameters

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| volumeID | path | string | yes | — | — |

#### Responses

| Status | Description | Content type | Schema |
|---|---|---|---|
| 200 | Successfully retrieved a team volume | application/json | VolumeAndToken |
| 401 | Authentication error | application/json | Error |
| 404 | Not found | application/json | Error |
| 500 | Server error | application/json | Error |

#### Data structures

##### VolumeAndToken

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `volumeID` | string | yes | — | ID of the volume |
| `name` | string | yes | — | Name of the volume |
| `token` | string | yes | — | Auth token to use for interacting with volume content |
| `domain` | string | no | — | Domain to use as the destination for volume content requests, replacing the default `api.<E2B_DOMAIN>`. Only returned when the team is connected to a custom (BYOC) cluster; absent otherwise, in which case the default domain is used.  |

##### Error

| Field | Type / schema | Required | Constraints | Description |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |

# E2B SDK 兼容 API 清单

本文说明云沙箱数据面围绕 E2B SDK / CLI 兼容的主要能力边界。这里的“API”指 E2B SDK 对象方法和 E2B CLI 命令，不是云沙箱原生控制面 OpenAPI。云沙箱未提供独立的数据面 SDK；Team、API Key、Quota 等资源管理能力可通过原生 OpenAPI、阿里云 SDK 或阿里云 CLI 调用，详情参见[阿里云 SDK & API 说明](04.阿里云%20SDK%20%26%20API%20说明.md)。

阅读本文时需要注意三点：

- 方法名和参数形态以当前使用的 E2B SDK 版本为准。Python 和 TypeScript 的命名风格可能不同。
- 标记为“兼容”的能力，仍需要结合账号配额、地域、模板状态和 SDK 版本验证。
- 标记为“受限”或“暂不兼容”的能力，不应作为生产接入主路径。

## Sandbox

Sandbox 是云沙箱的核心对象。当前主路径是通过 E2B SDK 创建、连接、查询、暂停、恢复、设置超时和终止 Sandbox。

兼容能力：

- 创建 Sandbox：`Sandbox.create()`。
- 连接已有 Sandbox：`Sandbox.connect(sandboxId)`。
- 查询 Sandbox 列表：`Sandbox.list()`。
- 查询 Sandbox 信息：`Sandbox.getInfo(sandboxId)`、`sandbox.getInfo()`。
- 判断运行状态：`sandbox.isRunning()`。
- 设置超时时间：`Sandbox.setTimeout(sandboxId, ms)`、`sandbox.setTimeout(ms)`。
- 暂停 Sandbox（需先开通白名单）：`sandbox.pause()`。
- 终止 Sandbox：`Sandbox.kill(sandboxId)`、`sandbox.kill()`。
- 获取文件上传 / 下载地址：`sandbox.uploadUrl(path)`、`sandbox.downloadUrl(path)`。
- 获取端口访问地址：`sandbox.getHost(port)`。
- 创建 Snapshot：`sandbox.createSnapshot()` / `Sandbox.createSnapshot()`。
- 列出 Snapshot：`Sandbox.listSnapshots()`。
- 删除 Snapshot：`Sandbox.deleteSnapshot()`。

使用建议：

- 业务代码应在 `try/finally` 中释放 Sandbox，避免任务异常时资源持续占用。
- `Sandbox.connect(sandboxId)` 适合恢复或接管已有 Sandbox。目标 Sandbox 已终止、超时回收或不属于当前账号 / 地域时，连接会失败。
- 端口访问能力适合预览临时 Web 服务，不应直接替代正式 API 网关、鉴权和流量治理。
- `Sandbox.create()` 的模板参数还可以是 Snapshot ID 或有名 Snapshot 全名。Snapshot 使用限制参见[快照](../04.功能说明/01.沙箱/12.快照.md)。

## Commands

Commands 用于在 Sandbox 内执行命令、管理后台进程和处理输入输出。

兼容能力：

- 执行命令：`sandbox.commands.run()`。
- 查询命令 / 进程列表：`sandbox.commands.list()`。
- 连接后台命令：`sandbox.commands.connect()`。
- 向命令发送标准输入：`sandbox.commands.sendStdin()`。
- 终止命令：`sandbox.commands.kill()`。
- 交互式终端：使用独立的 `sandbox.pty` API。

使用建议：

- 普通批处理命令优先使用 `commands.run()`；只有交互式 CLI、彩色输出、进度条或依赖 TTY 判断的命令才使用 `sandbox.pty`。
- 长任务建议设置命令级超时，并处理 stdout / stderr，避免业务侧只等待最终结果。
- 不要把未经校验的用户输入直接拼接进 shell 命令。

## Filesystem

Filesystem 用于在 Sandbox 生命周期内读写文件和管理目录。

兼容能力：

- 列出目录：`sandbox.files.list()`。
- 判断路径是否存在：`sandbox.files.exists()`。
- 获取文件或目录元信息：`sandbox.files.getInfo()` / `sandbox.files.get_info()`。
- 读取文件：`sandbox.files.read()`。
- 写入文件：`sandbox.files.write()`。
- 创建目录：`sandbox.files.makeDir()`。
- 删除文件或目录：`sandbox.files.remove()`。
- 重命名：`sandbox.files.rename()`。
- 目录监听：`sandbox.files.watchDir()` / `sandbox.files.watch_dir()`。

使用建议：

- 云沙箱当前不支持文件自定义元数据。即使 SDK 的 `files.write()` 方法签名接受 `metadata`，调用仍可能因 Sandbox 的 `envd` 版本低于 `0.6.2` 而在请求发送前失败。普通文件读写不受影响。
- 目录监听可用于等待文件生成或同步任务产物。只等待单个文件时，也可以使用 `exists()` 做有限次数轮询。
- Sandbox 本地文件系统适合任务临时文件，不应作为长期持久化存储。
- 跨 Sandbox 保留、共享或长期保存的数据，应写入 NAS、OSS 等外部存储。
- 处理用户上传路径时，应限制可写目录和文件大小，避免路径穿越和资源耗尽。

## Code Interpreter

Code Interpreter 用于执行代码片段，并在多次执行之间保持上下文。

兼容能力：

- 执行代码：`sandbox.runCode()` / `sandbox.run_code()`。
- 创建执行上下文：`sandbox.createCodeContext()` / `sandbox.create_code_context()`。
- 查询执行上下文：`sandbox.listCodeContexts()` / `sandbox.list_code_contexts()`。
- 重启执行上下文：`sandbox.restartCodeContext()` / `sandbox.restart_code_context()`。
- 删除执行上下文：`sandbox.removeCodeContext()` / `sandbox.remove_code_context()`。
- 获取执行输出，包括 stdout、stderr、execution count、表达式结果和 `execution.results` 富结果。
- stdout、stderr 和结果回调。
- 通过 `language` 参数执行 Python、JavaScript、TypeScript 和 Bash。不支持 Java 和 R。
- 在同一 Context 内保持变量和执行状态。

使用建议：

- 数据分析、代码解释器和 Notebook 风格任务适合使用 Code Interpreter。
- 如果任务更接近固定脚本执行，直接使用 Commands 更容易做审计、超时控制和输出校验。
- 不要默认允许代码访问任意路径、任意网络或无限资源。

## Templates

Templates 用于沉淀可复用运行环境。当前主路径是复用内置模板或构建自定义模板，再通过 SDK / CLI 创建 Sandbox。

兼容能力：

- 查询模板。
- 创建 / 更新 / 删除模板。
- 构建模板。
- 使用标签、版本或别名管理模板。
- 基于模板创建 Sandbox。

使用建议：

- 生产环境应固定模板名称、版本或别名，避免“latest”类引用造成不可控变更。
- 语言运行时、系统依赖、基础 CLI 和稳定业务 SDK 应前置到模板中，减少每次 Sandbox 启动后的临时安装。
- 模板构建依赖镜像仓库、网络和账号权限；构建失败时应先排查这些基础条件，再排查 SDK 参数。

## CLI

云沙箱兼容 E2B CLI 的常用调试和管理命令。CLI 适合本地验证、排障和轻量管理，不建议作为生产系统的主控制面。

兼容命令：

- 查看模板：`e2b template list`。
- 创建 Sandbox：`e2b sandbox create`。
- 查看 Sandbox：`e2b sandbox list`。
- 终止 Sandbox：`e2b sandbox kill <sandbox-id>`。
- 连接 Sandbox：`e2b sandbox connect <sandbox-id>`。
- 执行命令：`e2b sandbox exec <sandbox-id> <command>`。
- 查看指标：`e2b sandbox metrics <sandbox-id>`。

配置要求：

```bash
export E2B_API_KEY="<your-api-key>"
export E2B_API_URL="https://api.<region>.e2b.fc.aliyuncs.com"
export E2B_DOMAIN="<region>.e2b.fc.aliyuncs.com"
```

`E2B_ACCESS_TOKEN` 是 E2B 已废弃的旧认证变量。新版本 E2B CLI 应使用 `E2B_API_KEY`，不要在新接入流程中依赖 `E2B_ACCESS_TOKEN`。

CLI 命令参数可能随 E2B CLI 版本变化。命令行为与文档不一致时，先执行 `e2b --version` 和对应命令的 `--help`，确认本机 CLI 版本和参数形态。

## 受限能力

以下 E2B SDK 能力当前用于保持调用兼容或返回占位结果，不适合作为生产依赖：

- Sandbox Logs：可能返回空结果，不应用于正式日志采集。
- Network Config Update：支持运行中沙箱的出站规则及 `rules` 更新，省略的可更新字段会清空；字段限制见[网络访问控制](../04.功能说明/06.网络/05.网络访问控制.md)。

Sandbox Metrics 当前支持查看 CPU、内存指标；磁盘/页缓存字段会返回占位值，不建议用于容量判断。指标按 1 分钟粒度返回。计费、告警和容量治理应以函数计算控制台、云监控或日志服务中的正式数据为准。

如果业务需要日志、网络变更、VPC 接入或云上存储访问，应按云沙箱当前开放能力和阿里云基础设施方案单独设计，不要直接照搬 E2B 对应 API。

## 暂不兼容能力

以下能力当前不属于云沙箱 E2B 兼容主路径：

- Volume API。
- Team 管理（请在函数计算控制台管理 Team）。
- API Key / Access Token 管理。
- E2B 托管 MCP Gateway。
- E2B 自定义域名、代理隧道、Bring Your Own Cloud 等平台侧能力。

迁移已有 E2B 应用时，应先识别是否依赖上述能力。如果依赖，需要先调整架构或确认云沙箱是否已有替代方案，再进入开发接入。

## 上游内容存在不等于 FC 支持

E2B 官方还定义了 `autoPauseMemory`、`iam`、SOCKS5 代理、完整快照、Volume、Secret、托管 MCP、BYOC 等能力。接入 FC 前应查兼容清单和对应专题，不能据此视为 FC 已经支持。

文件自定义 `metadata` 与沙箱 `metadata` 是不同能力。FC 对文件 metadata 的 envd 版本限制仍适用。文件预签名 URL 的上游流程也不能直接替代 FC 已记录的访问限制，参见[上传和下载文件](../04.%E5%8A%9F%E8%83%BD%E8%AF%B4%E6%98%8E/04.%E6%96%87%E4%BB%B6%E7%B3%BB%E7%BB%9F/03.%E4%B8%8A%E4%BC%A0%E5%92%8C%E4%B8%8B%E8%BD%BD%E6%96%87%E4%BB%B6.md)。

## HTTP 请求与响应字段

以下按 E2B 官方 OpenAPI 补充本页涉及的协议字段，核对日期为 2026-09-08。参数表中的必填约束作用于所属对象；可选对象内部仍可有必填字段。字段描述保留官方英文，避免改写协议含义。FC 的支持范围及差异以本页正文和[兼容清单](01.E2B%20SDK%20%E5%85%BC%E5%AE%B9%20API%20%E6%B8%85%E5%8D%95.md)为准，表中的上游默认值、状态码和新增字段不能直接视为 FC 行为。

### `GET /health`

本节用于核对上游协议，不表示 FC 开放该接口；请先核对前文兼容清单。

[官方协议来源](https://docs.e2b.dev/api-reference/envd/check-the-health-of-the-service)

**Endpoint:** `GET /health`

Servers: `https://sandbox.{domain}`.

Security (upstream OpenAPI): `[]`. Scheme details are provided in the linked official contract.

#### 请求参数

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| E2b-Sandbox-Id | header | string | yes | — | Identifier of the target sandbox. Routes the request to that sandbox's envd over the shared sandbox host. |
| E2b-Sandbox-Port | header | integer | yes | default=49983 | Port envd listens on inside the sandbox (default 49983). |

#### 响应

| 状态码 | 说明 | 内容类型 | 结构 |
|---|---|---|---|
| 204 | The service is healthy | none declared | — |
| 502 | Sandbox not found | application/json | object |



### `GET /metrics`

本节用于核对上游协议，不表示 FC 开放该接口；请先核对前文兼容清单。

[官方协议来源](https://docs.e2b.dev/api-reference/envd/service-stats)

**Endpoint:** `GET /metrics`

Servers: `https://sandbox.{domain}`.

Security (upstream OpenAPI): `[{"SandboxAccessTokenAuth": []}]`. Scheme details are provided in the linked official contract.

#### 请求参数

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| E2b-Sandbox-Id | header | string | yes | — | Identifier of the target sandbox. Routes the request to that sandbox's envd over the shared sandbox host. |
| E2b-Sandbox-Port | header | integer | yes | default=49983 | Port envd listens on inside the sandbox (default 49983). |

#### 响应

| 状态码 | 说明 | 内容类型 | 结构 |
|---|---|---|---|
| 200 | The resource usage metrics of the service | application/json | Metrics |
| 502 | Sandbox not found | application/json | object |

#### 数据结构

##### Metrics

Resource usage metrics

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `ts` | integer | no | format="int64" | Unix timestamp in UTC for current sandbox time |
| `cpu_count` | integer | no | — | Number of CPU cores |
| `cpu_used_pct` | number | no | format="float" | CPU usage percentage |
| `mem_total` | integer | no | format="int64" | Total virtual memory in bytes |
| `mem_used` | integer | no | format="int64" | Used virtual memory in bytes |
| `mem_cache` | integer | no | — | Cached memory (page cache) in bytes |
| `mem_total_mib` | integer | no | format="int64" | Total virtual memory in MiB |
| `mem_used_mib` | integer | no | format="int64" | Used virtual memory in MiB |
| `disk_used` | integer | no | format="int64" | Used disk space in bytes |
| `disk_total` | integer | no | format="int64" | Total disk space in bytes |



### `POST /sandboxes/{sandboxID}/snapshots`

本节用于核对上游协议，不表示 FC 开放该接口；请先核对前文兼容清单。

[官方协议来源](https://docs.e2b.dev/api-reference/sandboxes/create-snapshot)

**Endpoint:** `POST /sandboxes/{sandboxID}/snapshots`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### 请求参数

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| sandboxID | path | string | yes | — | — |

#### 请求体

Required: **yes**.

##### application/json

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `name` | string | no | — | Optional name for the snapshot template. If a snapshot template with this name already exists, a new build will be assigned to the existing template instead of creating a new one. |

#### 响应

| 状态码 | 说明 | 内容类型 | 结构 |
|---|---|---|---|
| 201 | Snapshot created successfully | application/json | SnapshotInfo |
| 400 | Bad request | application/json | Error |
| 401 | Authentication error | application/json | Error |
| 404 | Not found | application/json | Error |
| 500 | Server error | application/json | Error |

#### 数据结构

##### SandboxSnapshotRequest

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `name` | string | no | — | Optional name for the snapshot template. If a snapshot template with this name already exists, a new build will be assigned to the existing template instead of creating a new one. |

##### SnapshotInfo

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `snapshotID` | string | yes | — | Identifier of the snapshot template including the tag. Uses namespace/alias when a name was provided (e.g. team-slug/my-snapshot:default), otherwise falls back to the raw template ID (e.g. abc123:default). |
| `names` | array of string | yes | — | Full names of the snapshot template including team namespace and tag (e.g. team-slug/my-snapshot:v2) |

##### Error

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |



### `POST /sandboxes/{sandboxID}/fork`

本节用于核对上游协议，不表示 FC 开放该接口；请先核对前文兼容清单。

[官方协议来源](https://docs.e2b.dev/api-reference/sandboxes/fork-sandbox)

**Endpoint:** `POST /sandboxes/{sandboxID}/fork`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### 请求参数

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| sandboxID | path | string | yes | — | — |

#### 请求体

Required: **no**.

##### application/json

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `timeout` | integer | no | default=15; minimum=0; format="int32" | Time to live for the new forked sandboxes in seconds. |
| `count` | integer | no | default=1; minimum=1; maximum=100; format="int32" | Number of forked sandboxes to create. All forks boot from the same snapshot, so the snapshot is captured once regardless of count. Each fork succeeds or fails independently; the outcome of each is reported in its entry of the response list. |

#### 响应

| 状态码 | 说明 | 内容类型 | 结构 |
|---|---|---|---|
| 201 | The sandbox was snapshotted and the forks were attempted; each entry reports one fork's outcome | application/json | array of SandboxForkResult |
| 401 | Authentication error | application/json | Error |
| 404 | Not found | application/json | Error |
| 409 | Conflict | application/json | Error |
| 500 | Server error | application/json | Error |
| 503 | Service unavailable | application/json | Error |

#### 数据结构

##### SandboxForkRequest

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `timeout` | integer | no | default=15; minimum=0; format="int32" | Time to live for the new forked sandboxes in seconds. |
| `count` | integer | no | default=1; minimum=1; maximum=100; format="int32" | Number of forked sandboxes to create. All forks boot from the same snapshot, so the snapshot is captured once regardless of count. Each fork succeeds or fails independently; the outcome of each is reported in its entry of the response list. |

##### SandboxForkResult

Result of one requested fork. Exactly one of sandbox or error is set: sandbox when the fork started successfully, error when it failed to start.

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `sandbox` | Sandbox | no | — | — |
| `error` | Error | no | — | — |

##### Sandbox

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `templateID` | string | yes | — | Identifier of the template from which is the sandbox created |
| `sandboxID` | string | yes | — | Identifier of the sandbox |
| `alias` | string | no | — | Alias of the template |
| `clientID` | string | yes | deprecated=true | Identifier of the client |
| `envdVersion` | EnvdVersion | yes | — | Version of the envd running in the sandbox |
| `envdAccessToken` | string / null | no | — | Access token for authenticating envd requests to this sandbox. Only returned when the sandbox is created with `secure: true`. Null for non-secure sandboxes (envd endpoints work without auth). |
| `trafficAccessToken` | string / null | no | — | Token required for accessing sandbox via proxy. |
| `domain` | string / null | no | deprecated=true | Deprecated: always null. Construct sandbox URLs as `https://{port}-{sandboxID}.e2b.app`. |

##### Error

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |

##### EnvdVersion

Version of the envd running in the sandbox

Type: `string`. Constraints: —. See the official contract linked above.



### `GET /snapshots`

本节用于核对上游协议，不表示 FC 开放该接口；请先核对前文兼容清单。

[官方协议来源](https://docs.e2b.dev/api-reference/sandboxes/list-snapshots)

**Endpoint:** `GET /snapshots`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### 请求参数

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| sandboxID | query | string | no | — | Filter snapshots by source sandbox ID |
| name | query | string | no | — | Filter snapshots by name or ID, optionally tag-qualified (e.g. "my-snapshot", "my-team/my-snapshot" or "my-snapshot:v1"). |
| limit | query | integer | no | default=100; minimum=1; maximum=100; format="int32" | Maximum number of items to return per page |
| nextToken | query | string | no | — | Cursor to start the list from |

#### 响应

| 状态码 | 说明 | 内容类型 | 结构 |
|---|---|---|---|
| 200 | Successfully returned snapshots | application/json | array of SnapshotInfo |
| 401 | Authentication error | application/json | Error |
| 500 | Server error | application/json | Error |

#### 数据结构

##### SnapshotInfo

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `snapshotID` | string | yes | — | Identifier of the snapshot template including the tag. Uses namespace/alias when a name was provided (e.g. team-slug/my-snapshot:default), otherwise falls back to the raw template ID (e.g. abc123:default). |
| `names` | array of string | yes | — | Full names of the snapshot template including team namespace and tag (e.g. team-slug/my-snapshot:v2) |

##### Error

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |



### `POST /secrets`

本节用于核对上游协议，不表示 FC 开放该接口；请先核对前文兼容清单。

[官方协议来源](https://docs.e2b.dev/api-reference/secrets/create-a-secret)

**Endpoint:** `POST /secrets`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### 请求体

Required: **yes**.

##### application/json

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `name` | string | yes | minLength=1; maxLength=128; pattern="^[a-zA-Z0-9_-]+$" | Name of the secret, unique within the project. Names are lower-cased before storage and returned in that canonical form; the sec_ prefix is reserved for secret identifiers.  |
| `value` | string | yes | — | Runtime marker stored as the secret's first version. The runtime resolves it to a value at sandbox egress. |
| `metadata` | SecretMetadata | no | — | Customer metadata of the secret. Always present, empty when unset. At most 32 entries; keys are limited to 128 bytes, values to 1024 bytes, and a secret's metadata to 8192 bytes in total.  |

#### 响应

| 状态码 | 说明 | 内容类型 | 结构 |
|---|---|---|---|
| 201 | Successfully created the secret | application/json | Secret |
| 400 | Bad request | application/json | Error |
| 401 | Authentication error | application/json | Error |
| 403 | Forbidden | application/json | Error |
| 404 | Not found | application/json | Error |
| 409 | Conflict | application/json | Error |
| 429 | Too many requests | application/json | Error |
| 500 | Server error | application/json | Error |
| 502 | Backend error | application/json | Error |
| 504 | Backend timeout | application/json | Error |

#### 数据结构

##### NewSecret

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `name` | string | yes | minLength=1; maxLength=128; pattern="^[a-zA-Z0-9_-]+$" | Name of the secret, unique within the project. Names are lower-cased before storage and returned in that canonical form; the sec_ prefix is reserved for secret identifiers.  |
| `value` | string | yes | — | Runtime marker stored as the secret's first version. The runtime resolves it to a value at sandbox egress. |
| `metadata` | SecretMetadata | no | — | Customer metadata of the secret. Always present, empty when unset. At most 32 entries; keys are limited to 128 bytes, values to 1024 bytes, and a secret's metadata to 8192 bytes in total.  |

##### Secret

Metadata of a secret. It never carries the secret value.

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `secretID` | string | yes | — | Identifier of the secret |
| `name` | string | yes | — | Name of the secret, unique within the project |
| `currentVersion` | integer | yes | format="int64" | Version served to readers that do not name one |
| `metadata` | SecretMetadata | yes | — | Customer metadata of the secret. Always present, empty when unset. At most 32 entries; keys are limited to 128 bytes, values to 1024 bytes, and a secret's metadata to 8192 bytes in total.  |
| `createdAt` | string | yes | format="date-time" | Time when the secret was created |
| `updatedAt` | string | yes | format="date-time" | Time when the secret was last updated |

##### SecretMetadata

Customer metadata of the secret. Always present, empty when unset. At most 32 entries; keys are limited to 128 bytes, values to 1024 bytes, and a secret's metadata to 8192 bytes in total.


Type: `object`. Constraints: —. See the official contract linked above.

##### Error

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |



### `DELETE /secrets/{secretID}`

本节用于核对上游协议，不表示 FC 开放该接口；请先核对前文兼容清单。

[官方协议来源](https://docs.e2b.dev/api-reference/secrets/delete-a-secret)

**Endpoint:** `DELETE /secrets/{secretID}`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### 请求参数

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| secretID | path | string | yes | — | Identifier of the secret (sec_ prefixed), or its canonical lower-case name  |

#### 响应

| 状态码 | 说明 | 内容类型 | 结构 |
|---|---|---|---|
| 204 | Successfully deleted the secret | none declared | — |
| 400 | Bad request | application/json | Error |
| 401 | Authentication error | application/json | Error |
| 403 | Forbidden | application/json | Error |
| 404 | Not found | application/json | Error |
| 409 | Conflict | application/json | Error |
| 429 | Too many requests | application/json | Error |
| 500 | Server error | application/json | Error |
| 502 | Backend error | application/json | Error |
| 504 | Backend timeout | application/json | Error |

#### 数据结构

##### Error

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |



### `GET /secrets/{secretID}`

本节用于核对上游协议，不表示 FC 开放该接口；请先核对前文兼容清单。

[官方协议来源](https://docs.e2b.dev/api-reference/secrets/get-a-secret)

**Endpoint:** `GET /secrets/{secretID}`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### 请求参数

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| secretID | path | string | yes | — | Identifier of the secret (sec_ prefixed), or its canonical lower-case name  |

#### 响应

| 状态码 | 说明 | 内容类型 | 结构 |
|---|---|---|---|
| 200 | Successfully retrieved the secret | application/json | Secret |
| 400 | Bad request | application/json | Error |
| 401 | Authentication error | application/json | Error |
| 403 | Forbidden | application/json | Error |
| 404 | Not found | application/json | Error |
| 409 | Conflict | application/json | Error |
| 429 | Too many requests | application/json | Error |
| 500 | Server error | application/json | Error |
| 502 | Backend error | application/json | Error |
| 504 | Backend timeout | application/json | Error |

#### 数据结构

##### Secret

Metadata of a secret. It never carries the secret value.

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `secretID` | string | yes | — | Identifier of the secret |
| `name` | string | yes | — | Name of the secret, unique within the project |
| `currentVersion` | integer | yes | format="int64" | Version served to readers that do not name one |
| `metadata` | SecretMetadata | yes | — | Customer metadata of the secret. Always present, empty when unset. At most 32 entries; keys are limited to 128 bytes, values to 1024 bytes, and a secret's metadata to 8192 bytes in total.  |
| `createdAt` | string | yes | format="date-time" | Time when the secret was created |
| `updatedAt` | string | yes | format="date-time" | Time when the secret was last updated |

##### SecretMetadata

Customer metadata of the secret. Always present, empty when unset. At most 32 entries; keys are limited to 128 bytes, values to 1024 bytes, and a secret's metadata to 8192 bytes in total.


Type: `object`. Constraints: —. See the official contract linked above.

##### Error

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |



### `GET /secrets`

本节用于核对上游协议，不表示 FC 开放该接口；请先核对前文兼容清单。

[官方协议来源](https://docs.e2b.dev/api-reference/secrets/list-project-secrets)

**Endpoint:** `GET /secrets`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### 请求参数

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| nextToken | query | string | no | — | Cursor to start the list from |
| limit | query | integer | no | default=100; minimum=1; maximum=100; format="int32" | Maximum number of items to return per page |

#### 响应

| 状态码 | 说明 | 内容类型 | 结构 |
|---|---|---|---|
| 200 | Successfully listed the project's secrets | application/json | array of Secret |
| 400 | Bad request | application/json | Error |
| 401 | Authentication error | application/json | Error |
| 403 | Forbidden | application/json | Error |
| 404 | Not found | application/json | Error |
| 409 | Conflict | application/json | Error |
| 429 | Too many requests | application/json | Error |
| 500 | Server error | application/json | Error |
| 502 | Backend error | application/json | Error |
| 504 | Backend timeout | application/json | Error |

#### 数据结构

##### Secret

Metadata of a secret. It never carries the secret value.

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `secretID` | string | yes | — | Identifier of the secret |
| `name` | string | yes | — | Name of the secret, unique within the project |
| `currentVersion` | integer | yes | format="int64" | Version served to readers that do not name one |
| `metadata` | SecretMetadata | yes | — | Customer metadata of the secret. Always present, empty when unset. At most 32 entries; keys are limited to 128 bytes, values to 1024 bytes, and a secret's metadata to 8192 bytes in total.  |
| `createdAt` | string | yes | format="date-time" | Time when the secret was created |
| `updatedAt` | string | yes | format="date-time" | Time when the secret was last updated |

##### SecretMetadata

Customer metadata of the secret. Always present, empty when unset. At most 32 entries; keys are limited to 128 bytes, values to 1024 bytes, and a secret's metadata to 8192 bytes in total.


Type: `object`. Constraints: —. See the official contract linked above.

##### Error

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |



### `POST /secrets/{secretID}`

本节用于核对上游协议，不表示 FC 开放该接口；请先核对前文兼容清单。

[官方协议来源](https://docs.e2b.dev/api-reference/secrets/update-a-secret)

**Endpoint:** `POST /secrets/{secretID}`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### 请求参数

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| secretID | path | string | yes | — | Identifier of the secret (sec_ prefixed), or its canonical lower-case name  |

#### 请求体

Required: **yes**.

##### application/json

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `value` | string | yes | — | Runtime marker stored as the secret's new version. The runtime resolves it to a value at sandbox egress. |
| `metadata` | SecretMetadata | no | — | Customer metadata of the secret. Always present, empty when unset. At most 32 entries; keys are limited to 128 bytes, values to 1024 bytes, and a secret's metadata to 8192 bytes in total.  |

#### 响应

| 状态码 | 说明 | 内容类型 | 结构 |
|---|---|---|---|
| 200 | Successfully updated the secret | application/json | Secret |
| 400 | Bad request | application/json | Error |
| 401 | Authentication error | application/json | Error |
| 403 | Forbidden | application/json | Error |
| 404 | Not found | application/json | Error |
| 409 | Conflict | application/json | Error |
| 429 | Too many requests | application/json | Error |
| 500 | Server error | application/json | Error |
| 502 | Backend error | application/json | Error |
| 504 | Backend timeout | application/json | Error |

#### 数据结构

##### SecretUpdate

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `value` | string | yes | — | Runtime marker stored as the secret's new version. The runtime resolves it to a value at sandbox egress. |
| `metadata` | SecretMetadata | no | — | Customer metadata of the secret. Always present, empty when unset. At most 32 entries; keys are limited to 128 bytes, values to 1024 bytes, and a secret's metadata to 8192 bytes in total.  |

##### Secret

Metadata of a secret. It never carries the secret value.

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `secretID` | string | yes | — | Identifier of the secret |
| `name` | string | yes | — | Name of the secret, unique within the project |
| `currentVersion` | integer | yes | format="int64" | Version served to readers that do not name one |
| `metadata` | SecretMetadata | yes | — | Customer metadata of the secret. Always present, empty when unset. At most 32 entries; keys are limited to 128 bytes, values to 1024 bytes, and a secret's metadata to 8192 bytes in total.  |
| `createdAt` | string | yes | format="date-time" | Time when the secret was created |
| `updatedAt` | string | yes | format="date-time" | Time when the secret was last updated |

##### SecretMetadata

Customer metadata of the secret. Always present, empty when unset. At most 32 entries; keys are limited to 128 bytes, values to 1024 bytes, and a secret's metadata to 8192 bytes in total.


Type: `object`. Constraints: —. See the official contract linked above.

##### Error

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |



### `GET /teams/{teamID}/metrics/max`

本节用于核对上游协议，不表示 FC 开放该接口；请先核对前文兼容清单。

[官方协议来源](https://docs.e2b.dev/api-reference/teams/get-team-metrics-max)

**Endpoint:** `GET /teams/{teamID}/metrics/max`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### 请求参数

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| teamID | path | string | yes | — | — |
| start | query | integer | no | minimum=0; format="int64" | Unix timestamp for the start of the interval, in seconds, for which the metrics are returned. |
| end | query | integer | no | minimum=0; format="int64" | Unix timestamp for the end of the interval, in seconds, for which the metrics are returned. |
| metric | query | string | yes | enum=["concurrent_sandboxes", "sandbox_start_rate"] | Metric to retrieve the maximum value for |

#### 响应

| 状态码 | 说明 | 内容类型 | 结构 |
|---|---|---|---|
| 200 | Successfully returned the team metrics | application/json | MaxTeamMetric |
| 400 | Bad request | application/json | Error |
| 401 | Authentication error | application/json | Error |
| 403 | Forbidden | application/json | Error |
| 500 | Server error | application/json | Error |

#### 数据结构

##### MaxTeamMetric

Team metric with timestamp

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `timestamp` | string | yes | deprecated=true; format="date-time" | Timestamp of the metric entry |
| `timestampUnix` | integer | yes | format="int64" | Timestamp of the metric entry in Unix time (seconds since epoch) |
| `value` | number | yes | — | The maximum value of the requested metric in the given interval |

##### Error

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |



### `GET /teams/{teamID}/metrics`

本节用于核对上游协议，不表示 FC 开放该接口；请先核对前文兼容清单。

[官方协议来源](https://docs.e2b.dev/api-reference/teams/get-team-metrics)

**Endpoint:** `GET /teams/{teamID}/metrics`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### 请求参数

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| teamID | path | string | yes | — | — |
| start | query | integer | no | minimum=0; format="int64" | Unix timestamp for the start of the interval, in seconds, for which the metrics are returned. |
| end | query | integer | no | minimum=0; format="int64" | Unix timestamp for the end of the interval, in seconds, for which the metrics are returned. |

#### 响应

| 状态码 | 说明 | 内容类型 | 结构 |
|---|---|---|---|
| 200 | Successfully returned the team metrics | application/json | array of TeamMetric |
| 400 | Bad request | application/json | Error |
| 401 | Authentication error | application/json | Error |
| 403 | Forbidden | application/json | Error |
| 500 | Server error | application/json | Error |

#### 数据结构

##### TeamMetric

Team metric with timestamp

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `timestamp` | string | yes | deprecated=true; format="date-time" | Timestamp of the metric entry |
| `timestampUnix` | integer | yes | format="int64" | Timestamp of the metric entry in Unix time (seconds since epoch) |
| `concurrentSandboxes` | integer | yes | format="int32" | The number of concurrent sandboxes for the team |
| `sandboxStartRate` | number | yes | format="float" | Number of sandboxes started per second |

##### Error

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |



### `GET /teams`

本节用于核对上游协议，不表示 FC 开放该接口；请先核对前文兼容清单。

[官方协议来源](https://docs.e2b.dev/api-reference/teams/list-teams)

**Endpoint:** `GET /teams`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[]`. Scheme details are provided in the linked official contract.

#### 响应

| 状态码 | 说明 | 内容类型 | 结构 |
|---|---|---|---|
| 200 | Successfully returned all teams | application/json | array of Team |
| 401 | Authentication error | application/json | Error |
| 500 | Server error | application/json | Error |

#### 数据结构

##### Team

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `teamID` | string | yes | — | Identifier of the team |
| `name` | string | yes | — | Name of the team |
| `apiKey` | string | yes | — | API key for the team |
| `isDefault` | boolean | yes | — | Whether the team is the default team |

##### Error

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |



### `POST /volumes`

本节用于核对上游协议，不表示 FC 开放该接口；请先核对前文兼容清单。

[官方协议来源](https://docs.e2b.dev/api-reference/volumes/create-team-volume)

**Endpoint:** `POST /volumes`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### 请求体

Required: **yes**.

##### application/json

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `name` | string | yes | pattern="^[a-zA-Z0-9_-]+$" | Name of the volume |

#### 响应

| 状态码 | 说明 | 内容类型 | 结构 |
|---|---|---|---|
| 201 | Successfully created a new team volume | application/json | VolumeAndToken |
| 400 | Bad request | application/json | Error |
| 401 | Authentication error | application/json | Error |
| 500 | Server error | application/json | Error |

#### 数据结构

##### NewVolume

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `name` | string | yes | pattern="^[a-zA-Z0-9_-]+$" | Name of the volume |

##### VolumeAndToken

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `volumeID` | string | yes | — | ID of the volume |
| `name` | string | yes | — | Name of the volume |
| `token` | string | yes | — | Auth token to use for interacting with volume content |
| `domain` | string | no | — | Domain to use as the destination for volume content requests, replacing the default `api.<E2B_DOMAIN>`. Only returned when the team is connected to a custom (BYOC) cluster; absent otherwise, in which case the default domain is used.  |

##### Error

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |



### `DELETE /volumes/{volumeID}`

本节用于核对上游协议，不表示 FC 开放该接口；请先核对前文兼容清单。

[官方协议来源](https://docs.e2b.dev/api-reference/volumes/delete-team-volume)

**Endpoint:** `DELETE /volumes/{volumeID}`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### 请求参数

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| volumeID | path | string | yes | — | — |

#### 响应

| 状态码 | 说明 | 内容类型 | 结构 |
|---|---|---|---|
| 204 | Successfully deleted a team volume | none declared | — |
| 401 | Authentication error | application/json | Error |
| 404 | Not found | application/json | Error |
| 500 | Server error | application/json | Error |

#### 数据结构

##### Error

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |



### `GET /volumes`

本节用于核对上游协议，不表示 FC 开放该接口；请先核对前文兼容清单。

[官方协议来源](https://docs.e2b.dev/api-reference/volumes/list-team-volumes)

**Endpoint:** `GET /volumes`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### 响应

| 状态码 | 说明 | 内容类型 | 结构 |
|---|---|---|---|
| 200 | Successfully listed all team volumes | application/json | array of Volume |
| 401 | Authentication error | application/json | Error |
| 500 | Server error | application/json | Error |

#### 数据结构

##### Volume

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `volumeID` | string | yes | — | ID of the volume |
| `name` | string | yes | — | Name of the volume |

##### Error

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |



### `GET /volumes/{volumeID}`

本节用于核对上游协议，不表示 FC 开放该接口；请先核对前文兼容清单。

[官方协议来源](https://docs.e2b.dev/api-reference/volumes/team-volume)

**Endpoint:** `GET /volumes/{volumeID}`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### 请求参数

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| volumeID | path | string | yes | — | — |

#### 响应

| 状态码 | 说明 | 内容类型 | 结构 |
|---|---|---|---|
| 200 | Successfully retrieved a team volume | application/json | VolumeAndToken |
| 401 | Authentication error | application/json | Error |
| 404 | Not found | application/json | Error |
| 500 | Server error | application/json | Error |

#### 数据结构

##### VolumeAndToken

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `volumeID` | string | yes | — | ID of the volume |
| `name` | string | yes | — | Name of the volume |
| `token` | string | yes | — | Auth token to use for interacting with volume content |
| `domain` | string | no | — | Domain to use as the destination for volume content requests, replacing the default `api.<E2B_DOMAIN>`. Only returned when the team is connected to a custom (BYOC) cluster; absent otherwise, in which case the default domain is used.  |

##### Error

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |

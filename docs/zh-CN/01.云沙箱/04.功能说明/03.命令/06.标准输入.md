# 标准输入

标准输入用于向运行中的命令发送数据，适合交互式程序、脚本参数输入和长进程控制。

## 启动可接收 stdin 的命令

TypeScript 示例：

```typescript
const handle = await sandbox.commands.run("cat", {
  background: true,
  stdin: true,
});

await sandbox.commands.sendStdin(handle.pid, "hello from stdin\n");
await sandbox.commands.closeStdin(handle.pid);

const result = await handle.wait();
console.log(result.stdout);
```

Python 示例：

```python
handle = sandbox.commands.run(
    "cat",
    background=True,
    stdin=True,
)

sandbox.commands.send_stdin(handle.pid, "hello from stdin\n")
sandbox.commands.close_stdin(handle.pid)

result = handle.wait()
print(result.stdout)
```

`closeStdin()` 会关闭非 PTY 命令的标准输入，并向进程发送 EOF。PTY 会话不使用 `closeStdin()`；如果需要在 PTY 中结束输入，可以发送 Ctrl+D（`0x04`）或按交互式程序约定发送退出命令。

## 使用建议

- 对简单参数，优先使用命令参数或环境变量。
- 对需要持续输入的进程，保存 `pid`，任务正常结束时优先关闭 stdin 并等待结果；超时或异常时再主动 `kill()`。
- 不要把敏感凭证直接写入命令输出可见的交互流程。

## HTTP 请求与响应字段

以下按 E2B 官方 OpenAPI 补充本页涉及的协议字段，核对日期为 2026-09-08。参数表中的必填约束作用于所属对象；可选对象内部仍可有必填字段。字段描述保留官方英文，避免改写协议含义。FC 的支持范围及差异以本页正文和[兼容清单](../../07.%E5%BC%80%E5%8F%91%E5%8F%82%E8%80%83/01.E2B%20SDK%20%E5%85%BC%E5%AE%B9%20API%20%E6%B8%85%E5%8D%95.md)为准，表中的上游默认值、状态码和新增字段不能直接视为 FC 行为。

### `POST /process.Process/CloseStdin`



[官方协议来源](https://docs.e2b.dev/api-reference/process/closestdin)

**Endpoint:** `POST /process.Process/CloseStdin`

Servers: `https://sandbox.{domain}`.

Security (upstream OpenAPI): `[{"SandboxAccessTokenAuth": []}]`. Scheme details are provided in the linked official contract.

#### 请求参数

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| E2b-Sandbox-Id | header | string | yes | — | Identifier of the target sandbox. Routes the request to that sandbox's envd over the shared sandbox host. |
| E2b-Sandbox-Port | header | integer | yes | default=49983 | Port envd listens on inside the sandbox (default 49983). |
| Connect-Protocol-Version | header | connect-protocol-version | yes | const=1 | Define the version of the Connect protocol |
| Connect-Timeout-Ms | header | connect-timeout-header | no | — | Define the timeout, in ms |

#### 请求体

Required: **yes**.

##### application/json

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `process` | process.ProcessSelector | no | — | — |

#### 响应

| 状态码 | 说明 | 内容类型 | 结构 |
|---|---|---|---|
| 200 | Success | application/json | process.CloseStdinResponse |
| 502 | Sandbox not found | application/json | object |

#### 数据结构

##### connect-protocol-version

Define the version of the Connect protocol

Type: `number`. Constraints: const=1. See the official contract linked above.

##### connect-timeout-header

Define the timeout, in ms

Type: `number`. Constraints: —. See the official contract linked above.

##### process.CloseStdinRequest

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `process` | process.ProcessSelector | no | — | — |

##### process.CloseStdinResponse

Type: `object`. Constraints: —. See the official contract linked above.

##### process.ProcessSelector

Type: `oneOf: see schema, see schema`. Constraints: —. See the official contract linked above.



### `POST /process.Process/SendInput`



[官方协议来源](https://docs.e2b.dev/api-reference/process/sendinput)

**Endpoint:** `POST /process.Process/SendInput`

Servers: `https://sandbox.{domain}`.

Security (upstream OpenAPI): `[{"SandboxAccessTokenAuth": []}]`. Scheme details are provided in the linked official contract.

#### 请求参数

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| E2b-Sandbox-Id | header | string | yes | — | Identifier of the target sandbox. Routes the request to that sandbox's envd over the shared sandbox host. |
| E2b-Sandbox-Port | header | integer | yes | default=49983 | Port envd listens on inside the sandbox (default 49983). |
| Connect-Protocol-Version | header | connect-protocol-version | yes | const=1 | Define the version of the Connect protocol |
| Connect-Timeout-Ms | header | connect-timeout-header | no | — | Define the timeout, in ms |

#### 请求体

Required: **yes**.

##### application/json

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `process` | process.ProcessSelector | no | — | — |
| `input` | process.ProcessInput | no | — | — |

#### 响应

| 状态码 | 说明 | 内容类型 | 结构 |
|---|---|---|---|
| 200 | Success | application/json | process.SendInputResponse |
| 502 | Sandbox not found | application/json | object |

#### 数据结构

##### connect-protocol-version

Define the version of the Connect protocol

Type: `number`. Constraints: const=1. See the official contract linked above.

##### connect-timeout-header

Define the timeout, in ms

Type: `number`. Constraints: —. See the official contract linked above.

##### process.SendInputRequest

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `process` | process.ProcessSelector | no | — | — |
| `input` | process.ProcessInput | no | — | — |

##### process.SendInputResponse

Type: `object`. Constraints: —. See the official contract linked above.

##### process.ProcessSelector

Type: `oneOf: see schema, see schema`. Constraints: —. See the official contract linked above.

##### process.ProcessInput

Type: `oneOf: see schema, see schema`. Constraints: —. See the official contract linked above.



### `POST /process.Process/StreamInput`



[官方协议来源](https://docs.e2b.dev/api-reference/process/streaminput)

**Endpoint:** `POST /process.Process/StreamInput`

Servers: `https://sandbox.{domain}`.

Security (upstream OpenAPI): `[{"SandboxAccessTokenAuth": []}]`. Scheme details are provided in the linked official contract.

#### 请求参数

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| Connect-Protocol-Version | header | connect-protocol-version | yes | const=1 | Define the version of the Connect protocol |
| Connect-Timeout-Ms | header | connect-timeout-header | no | — | Define the timeout, in ms |
| E2b-Sandbox-Id | header | string | yes | — | Identifier of the target sandbox. Routes the request to that sandbox's envd over the shared sandbox host. |
| E2b-Sandbox-Port | header | integer | yes | default=49983 | Port envd listens on inside the sandbox (default 49983). |

#### 请求体

Required: **yes**.

##### application/connect+json

Type: `oneOf: see schema, see schema, see schema`. Constraints: —. See the official contract linked above.

#### 响应

| 状态码 | 说明 | 内容类型 | 结构 |
|---|---|---|---|
| 200 | Stream of StreamInputResponse events | application/connect+json | process.StreamInputResponse |
| 502 | Sandbox not found | application/json | object |

#### 数据结构

##### connect-protocol-version

Define the version of the Connect protocol

Type: `number`. Constraints: const=1. See the official contract linked above.

##### connect-timeout-header

Define the timeout, in ms

Type: `number`. Constraints: —. See the official contract linked above.

##### process.StreamInputRequest

Type: `oneOf: see schema, see schema, see schema`. Constraints: —. See the official contract linked above.

##### process.StreamInputResponse

Type: `object`. Constraints: —. See the official contract linked above.

##### process.StreamInputRequest.DataEvent

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `input` | process.ProcessInput | no | — | — |

##### process.StreamInputRequest.KeepAlive

Type: `object`. Constraints: —. See the official contract linked above.

##### process.StreamInputRequest.StartEvent

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `process` | process.ProcessSelector | no | — | — |

##### process.ProcessInput

Type: `oneOf: see schema, see schema`. Constraints: —. See the official contract linked above.

##### process.ProcessSelector

Type: `oneOf: see schema, see schema`. Constraints: —. See the official contract linked above.

# 连接沙箱

连接沙箱用于在不同进程、服务或函数之间复用同一个沙箱。典型场景是：请求 A 创建沙箱并返回 `sandboxId`，请求 B 再通过 `sandboxId` 继续执行任务。

## 保存沙箱 ID

```typescript
import { Sandbox } from "e2b";

const sandbox = await Sandbox.create("code-interpreter-v1", {
  apiKey: process.env.E2B_API_KEY,
  apiUrl: process.env.E2B_API_URL,
  domain: process.env.E2B_DOMAIN,
});

console.log(sandbox.sandboxId);
```

Python 示例：

```python
import os

from e2b import Sandbox

sandbox = Sandbox.create(
    template="code-interpreter-v1",
    api_key=os.environ["E2B_API_KEY"],
    api_url=os.environ["E2B_API_URL"],
    domain=os.environ["E2B_DOMAIN"],
)

print(sandbox.sandbox_id)
```

## 连接已有沙箱

```typescript
const sandbox = await Sandbox.connect(process.env.SANDBOX_ID, {
  apiKey: process.env.E2B_API_KEY,
  apiUrl: process.env.E2B_API_URL,
  domain: process.env.E2B_DOMAIN,
});

const result = await sandbox.commands.run("pwd");
console.log(result.stdout.trim());
```

Python 示例：

```python
sandbox = Sandbox.connect(
    os.environ["SANDBOX_ID"],
    api_key=os.environ["E2B_API_KEY"],
    api_url=os.environ["E2B_API_URL"],
    domain=os.environ["E2B_DOMAIN"],
)

result = sandbox.commands.run("pwd")
print(result.stdout.strip())
```

如果目标沙箱处于暂停状态，`Sandbox.connect()` 会自动恢复后再返回可用的沙箱对象。暂停与恢复功能当前仅对已开通白名单的账号可用。

## 使用建议

- `sandboxId` 是连接沙箱的关键标识，应由业务系统保存。
- 连接只恢复控制面和运行环境访问，不会替业务自动管理任务状态。
- 无法确认沙箱仍可用时，可以连接后调用 `sandbox.getInfo()` 或执行一次轻量命令做健康检查。

## 返回对象与 HTTP 响应

| 含义 | Python 对象 | TypeScript 对象 | HTTP 字段 | 出现条件 / 注意事项 |
| --- | --- | --- | --- | --- |
| 沙箱标识 | `sandbox_id` | `sandboxId` | `sandboxID` | 用于后续连接和管理 |
| 模板标识 | `template_id`（详情） | `templateId`（详情） | `templateID` | SDK 对象与详情对象不必具有相同字段集合 |
| 沙箱状态 | `state`（详情/列表项） | `state`（详情/列表项） | `state` | 按所属响应结构读取 |
| envd 令牌 | SDK 内部使用 | SDK 内部使用 | `envdAccessToken` | 与平台 API Key 不同，SDK 自动用于 envd 请求 |
| 公网流量令牌 | `traffic_access_token` | `trafficAccessToken` | `trafficAccessToken` | FC 入站受限沙箱在创建/连接时返回；GET/List 不回显，公开沙箱可省略 |
| 到期时间 | `end_at`（详情） | `endAt`（详情） | `endAt` | 日期时间；用于理解剩余 TTL |

E2B 官方 `connect` 对运行中沙箱返回 200，对恢复的沙箱返回 201；当前 FC 网关这两种成功路径均返回 200。SDK 返回的是 Sandbox 对象，不应把 HTTP 状态码写成 SDK 的返回类型。

E2B 当前的 `domain` 响应字段标为弃用且总为 null；不能据此假设 FC 的同名响应也恒为 null。业务访问地址优先通过 SDK 的 `getHost()` / `get_host()` 获取。字段的省略、null 和空值应分别处理。

## HTTP 请求与响应字段

以下按 E2B 官方 OpenAPI 补充本页涉及的协议字段，核对日期为 2026-09-08。参数表中的必填约束作用于所属对象；可选对象内部仍可有必填字段。字段描述保留官方英文，避免改写协议含义。FC 的支持范围及差异以本页正文和[兼容清单](../../07.%E5%BC%80%E5%8F%91%E5%8F%82%E8%80%83/01.E2B%20SDK%20%E5%85%BC%E5%AE%B9%20API%20%E6%B8%85%E5%8D%95.md)为准，表中的上游默认值、状态码和新增字段不能直接视为 FC 行为。

### `POST /sandboxes/{sandboxID}/connect`



[官方协议来源](https://docs.e2b.dev/api-reference/sandboxes/connect-to-sandbox)

**Endpoint:** `POST /sandboxes/{sandboxID}/connect`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### 请求参数

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| sandboxID | path | string | yes | — | — |

#### 请求体

Required: **yes**.

##### application/json

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `timeout` | integer | yes | minimum=0; format="int32" | Timeout in seconds from the current time after which the sandbox should expire |
| `memory` | boolean | no | — | Defaults to true. When false and the sandbox is paused, resume from disk state only: the sandbox cold-boots fresh and any memory in the snapshot is ignored, never modified or deleted. Disk state has crash-recovery semantics — writes not flushed before the pause may be lost. A no-op for snapshots that contain no memory. Rejected with an error in environments where this capability is not enabled, never silently downgraded to a memory restore. |

#### 响应

| 状态码 | 说明 | 内容类型 | 结构 |
|---|---|---|---|
| 200 | The sandbox was already running | application/json | Sandbox |
| 201 | The sandbox was resumed successfully | application/json | Sandbox |
| 400 | Bad request | application/json | Error |
| 401 | Authentication error | application/json | Error |
| 404 | Not found | application/json | Error |
| 409 | Conflict | application/json | Error |
| 500 | Server error | application/json | Error |
| 503 | Service unavailable | application/json | Error |
| 504 | Backend timeout | application/json | Error |

#### 数据结构

##### ConnectSandbox

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `timeout` | integer | yes | minimum=0; format="int32" | Timeout in seconds from the current time after which the sandbox should expire |
| `memory` | boolean | no | — | Defaults to true. When false and the sandbox is paused, resume from disk state only: the sandbox cold-boots fresh and any memory in the snapshot is ignored, never modified or deleted. Disk state has crash-recovery semantics — writes not flushed before the pause may be lost. A no-op for snapshots that contain no memory. Rejected with an error in environments where this capability is not enabled, never silently downgraded to a memory restore. |

##### Sandbox

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `templateID` | string | yes | — | Identifier of the template from which is the sandbox created |
| `sandboxID` | string | yes | — | Identifier of the sandbox |
| `alias` | string | no | — | Alias of the template |
| `clientID` | string | yes | deprecated=true | Identifier of the client |
| `envdVersion` | EnvdVersion | yes | — | Version of the envd running in the sandbox |
| `envdAccessToken` | string / null | no | — | Access token for authenticating envd requests to this sandbox. Only returned when the sandbox is created with `secure: true`. Null for non-secure sandboxes (envd endpoints work without auth). |
| `trafficAccessToken` | string / null | no | — | Token required for accessing sandbox via proxy. |
| `domain` | string / null | no | deprecated=true | Deprecated: always null. Construct sandbox URLs as `https://{port}-{sandboxID}.e2b.app`. |

##### EnvdVersion

Version of the envd running in the sandbox

Type: `string`. Constraints: —. See the official contract linked above.

##### Error

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |



### `GET /sandboxes/{sandboxID}`



[官方协议来源](https://docs.e2b.dev/api-reference/sandboxes/get-sandbox)

**Endpoint:** `GET /sandboxes/{sandboxID}`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### 请求参数

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| sandboxID | path | string | yes | — | — |

#### 响应

| 状态码 | 说明 | 内容类型 | 结构 |
|---|---|---|---|
| 200 | Successfully returned the sandbox | application/json | SandboxDetail |
| 401 | Authentication error | application/json | Error |
| 404 | Not found | application/json | Error |
| 500 | Server error | application/json | Error |

#### 数据结构

##### SandboxDetail

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `templateID` | string | yes | — | Identifier of the template from which is the sandbox created |
| `alias` | string | no | — | Alias of the template |
| `sandboxID` | string | yes | — | Identifier of the sandbox |
| `clientID` | string | yes | deprecated=true | Identifier of the client |
| `startedAt` | string | yes | format="date-time" | Time when the sandbox was started |
| `endAt` | string | yes | format="date-time" | Time when the sandbox will expire |
| `envdVersion` | EnvdVersion | yes | — | Version of the envd running in the sandbox |
| `envdAccessToken` | string / null | no | — | Access token for authenticating envd requests to this sandbox. Only returned when the sandbox is created with `secure: true`. Null for non-secure sandboxes (envd endpoints work without auth). |
| `allowInternetAccess` | boolean / null | no | — | Whether internet access was explicitly enabled or disabled for the sandbox. Null means it was not explicitly set. |
| `domain` | string / null | no | deprecated=true | Deprecated: always null. Construct sandbox URLs as `https://{port}-{sandboxID}.e2b.app`. |
| `cpuCount` | CPUCount | yes | minimum=1; format="int32" | CPU cores for the sandbox |
| `memoryMB` | MemoryMB | yes | minimum=128; format="int32" | Memory for the sandbox in MiB |
| `diskSizeMB` | DiskSizeMB | yes | minimum=0; format="int32" | Disk size for the sandbox in MiB |
| `metadata` | SandboxMetadata | no | — | — |
| `state` | SandboxState | yes | enum=["running", "paused"] | State of the sandbox |
| `network` | SandboxNetworkConfig | no | — | — |
| `lifecycle` | SandboxLifecycle | no | — | Sandbox lifecycle policy returned by sandbox info. |
| `volumeMounts` | array of SandboxVolumeMount | no | — | — |

##### EnvdVersion

Version of the envd running in the sandbox

Type: `string`. Constraints: —. See the official contract linked above.

##### CPUCount

CPU cores for the sandbox

Type: `integer`. Constraints: minimum=1; format="int32". See the official contract linked above.

##### MemoryMB

Memory for the sandbox in MiB

Type: `integer`. Constraints: minimum=128; format="int32". See the official contract linked above.

##### DiskSizeMB

Disk size for the sandbox in MiB

Type: `integer`. Constraints: minimum=0; format="int32". See the official contract linked above.

##### SandboxMetadata

Type: `object`. Constraints: —. See the official contract linked above.

##### SandboxState

State of the sandbox

Type: `string`. Constraints: enum=["running", "paused"]. See the official contract linked above.

##### SandboxNetworkConfig

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `allowPublicTraffic` | boolean | no | default=true | Specify if the sandbox URLs should be accessible only with authentication. |
| `allowOut` | array of string | no | — | List of allowed destinations for egress traffic. Each entry can be a CIDR block (e.g. "8.8.8.8/32"), a bare IP address (e.g. "8.8.8.8"), or a domain name (e.g. "example.com", "*.example.com"). Allowed entries always take precedence over denied entries. |
| `denyOut` | array of string | no | — | List of denied CIDR blocks or IP addresses for egress traffic. Domain names are not supported for deny rules. |
| `egressProxy` | SandboxEgressProxyConfig | no | nullable=true | SOCKS5 proxy for sandbox egress. Outbound TCP is tunneled through the proxy after allow/deny filtering; the sandbox is unaware. Domain-matched flows use remote DNS (ATYP=domain). |
| `maskRequestHost` | string | no | — | Specify host mask which will be used for all sandbox requests |
| `rules` | object | no | — | Per-domain transform rules applied to matching outbound HTTPS requests. Keys may be exact DNS names (for example, "api.example.com") or a leading wildcard (for example, "*.example.com"), and are normalized to lowercase on write. Wildcards match subdomains at any depth but not the apex domain; a bare "*" is invalid. Exact rules take precedence, followed by the longest matching wildcard suffix, and matching rule sets are not merged. Broad wildcards such as "*.com" are allowed and may expose transformed credentials to every matching destination the sandbox contacts. Rules do not grant network access; configure allowOut separately to permit the destination.  |

##### SandboxLifecycle

Sandbox lifecycle policy returned by sandbox info.

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `autoResume` | boolean | yes | — | Whether the sandbox can auto-resume. |
| `onTimeout` | SandboxOnTimeout | yes | enum=["kill", "pause"] | Action taken when the sandbox times out. |

##### SandboxVolumeMount

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `name` | string | yes | — | Name of the volume |
| `path` | string | yes | — | Path of the volume |

##### Error

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |

##### SandboxEgressProxyConfig

SOCKS5 proxy for sandbox egress. Outbound TCP is tunneled through the proxy after allow/deny filtering; the sandbox is unaware. Domain-matched flows use remote DNS (ATYP=domain).

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `address` | string | yes | — | SOCKS5 proxy address in host:port format (e.g. "proxy.example.com:1080"). |
| `username` | string | no | maxLength=255 | Optional SOCKS5 username (RFC 1929), max 255 bytes. |
| `password` | string | no | maxLength=255 | Optional SOCKS5 password (RFC 1929), max 255 bytes. |

##### SandboxNetworkRule

Transform rule applied to egress requests matching a domain pattern.

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `transform` | SandboxNetworkTransform | no | — | Transformations applied to matching egress requests before forwarding. |

##### SandboxOnTimeout

Action taken when the sandbox times out.

Type: `string`. Constraints: enum=["kill", "pause"]. See the official contract linked above.

##### SandboxNetworkTransform

Transformations applied to matching egress requests before forwarding.

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `headers` | object | no | — | HTTP headers to inject or override in matching requests. An existing header with the same name is replaced. Values are plain strings; secret resolution happens client-side before sending to the API.  |

# 模板名称与版本

模板名称标识运行环境；标签为模板的某次构建标记阶段或版本（如 `prod`、`staging`、`v1`），便于灰度和回滚。

开始前，请先完成[快速开始](01.快速开始.md)中的连接配置。

## 模板名称

名称建议包含业务场景、语言或基础镜像、关键依赖版本和日期，便于识别和分组：

```text
agent-python313-20260704
code-review-node22-20260704
data-analysis-py313-v1
```

创建沙箱时用名称引用模板：

```typescript
const sandbox = await Sandbox.create("agent-python313-20260704");
```

Python 示例：

```python
sandbox = Sandbox.create(template="agent-python313-20260704")
```

## 标签

### 分配标签

`assignTags` 第一个参数使用 `name:tag` 格式指定从哪个构建打标签；不带 `:tag` 时默认指向 `default` 构建。

```typescript
import { Template } from "e2b";

// 不指定源标签时，为 default 构建分配标签
await Template.assignTags("agent-python313-20260704", ["staging", "v1"]);
```

Python 示例：

```python
from e2b import Template

Template.assign_tags("agent-python313-20260704", ["staging", "v1"])
```

### 查询标签

分配与移除标签按模板名称操作；查询标签时，TypeScript 用模板名称即可返回全部标签，Python 需使用模板 ID（例如 `Template.build` 返回的 `build.template_id`）。

```typescript
const tags = await Template.getTags("agent-python313-20260704");
console.log(tags);
// 输出: [{ tag: 'default', buildId: '...', createdAt: '...' },
//       { tag: 'staging', buildId: '...', createdAt: '...' }, ...]
```

Python 示例：

```python
tags = Template.get_tags("<template-id>")
print(tags)
# 输出: [TemplateTag(tag='default', build_id='...', created_at='...'), ...]
```

### 移除标签

```typescript
await Template.removeTags("agent-python313-20260704", "staging");
```

Python 示例：

```python
Template.remove_tags("agent-python313-20260704", "staging")
```

### 限制说明

- 每个模板最多 20 个标签。
- 不能删除 `default` 标签（返回 400 错误）。
- 重复分配同一标签、删除不存在的标签都是幂等操作。

## 使用建议

发布新版本时，先验证新模板或标签，再切换业务引用；复杂的发布规则仍由业务系统或发布系统管理。

## HTTP 请求与响应字段

以下按 E2B 官方 OpenAPI 补充本页涉及的协议字段，核对日期为 2026-09-08。参数表中的必填约束作用于所属对象；可选对象内部仍可有必填字段。字段描述保留官方英文，避免改写协议含义。FC 的支持范围及差异以本页正文和[兼容清单](../../07.%E5%BC%80%E5%8F%91%E5%8F%82%E8%80%83/01.E2B%20SDK%20%E5%85%BC%E5%AE%B9%20API%20%E6%B8%85%E5%8D%95.md)为准，表中的上游默认值、状态码和新增字段不能直接视为 FC 行为。

### `POST /templates/tags`



[官方协议来源](https://docs.e2b.dev/api-reference/tags/assign-tags)

**Endpoint:** `POST /templates/tags`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### 请求体

Required: **yes**.

##### application/json

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `target` | string | yes | — | Target template in "name:tag" format |
| `tags` | array of string | yes | — | Tags to assign to the template |

#### 响应

| 状态码 | 说明 | 内容类型 | 结构 |
|---|---|---|---|
| 201 | Tag assigned successfully | application/json | AssignedTemplateTags |
| 400 | Bad request | application/json | Error |
| 401 | Authentication error | application/json | Error |
| 404 | Not found | application/json | Error |
| 500 | Server error | application/json | Error |

#### 数据结构

##### AssignTemplateTagsRequest

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `target` | string | yes | — | Target template in "name:tag" format |
| `tags` | array of string | yes | — | Tags to assign to the template |

##### AssignedTemplateTags

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `tags` | array of string | yes | — | Assigned tags of the template |
| `buildID` | string | yes | format="uuid" | Identifier of the build associated with these tags |

##### Error

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |



### `DELETE /templates/tags`



[官方协议来源](https://docs.e2b.dev/api-reference/tags/delete-tags)

**Endpoint:** `DELETE /templates/tags`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### 请求体

Required: **yes**.

##### application/json

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `name` | string | yes | — | Name of the template |
| `tags` | array of string | yes | — | Tags to delete |

#### 响应

| 状态码 | 说明 | 内容类型 | 结构 |
|---|---|---|---|
| 204 | Tags deleted successfully | none declared | — |
| 400 | Bad request | application/json | Error |
| 401 | Authentication error | application/json | Error |
| 404 | Not found | application/json | Error |
| 500 | Server error | application/json | Error |

#### 数据结构

##### DeleteTemplateTagsRequest

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `name` | string | yes | — | Name of the template |
| `tags` | array of string | yes | — | Tags to delete |

##### Error

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |



### `GET /templates/{templateID}/tags`



[官方协议来源](https://docs.e2b.dev/api-reference/tags/list-template-tags)

**Endpoint:** `GET /templates/{templateID}/tags`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### 请求参数

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| templateID | path | string | yes | — | — |

#### 响应

| 状态码 | 说明 | 内容类型 | 结构 |
|---|---|---|---|
| 200 | Successfully returned the template tags | application/json | array of TemplateTag |
| 401 | Authentication error | application/json | Error |
| 403 | Forbidden | application/json | Error |
| 404 | Not found | application/json | Error |
| 500 | Server error | application/json | Error |

#### 数据结构

##### TemplateTag

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `tag` | string | yes | — | The tag name |
| `buildID` | string | yes | format="uuid" | Identifier of the build associated with this tag |
| `createdAt` | string | yes | format="date-time" | Time when the tag was assigned |

##### Error

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |



### `DELETE /templates/{templateID}`



[官方协议来源](https://docs.e2b.dev/api-reference/templates/delete-template)

**Endpoint:** `DELETE /templates/{templateID}`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### 请求参数

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| templateID | path | string | yes | — | — |

#### 响应

| 状态码 | 说明 | 内容类型 | 结构 |
|---|---|---|---|
| 204 | The template was deleted successfully | none declared | — |
| 401 | Authentication error | application/json | Error |
| 500 | Server error | application/json | Error |

#### 数据结构

##### Error

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |



### `GET /templates/aliases/{alias}`



[官方协议来源](https://docs.e2b.dev/api-reference/templates/get-template-by-alias)

**Endpoint:** `GET /templates/aliases/{alias}`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### 请求参数

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| alias | path | string | yes | — | Template alias |

#### 响应

| 状态码 | 说明 | 内容类型 | 结构 |
|---|---|---|---|
| 200 | Successfully queried template by alias | application/json | TemplateAliasResponse |
| 400 | Bad request | application/json | Error |
| 403 | Forbidden | application/json | Error |
| 404 | Not found | application/json | Error |
| 500 | Server error | application/json | Error |

#### 数据结构

##### TemplateAliasResponse

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `templateID` | string | yes | — | Identifier of the template |
| `public` | boolean | yes | — | Whether the template is public or only accessible by the team |

##### Error

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |



### `GET /templates/{templateID}`



[官方协议来源](https://docs.e2b.dev/api-reference/templates/get-template)

**Endpoint:** `GET /templates/{templateID}`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### 请求参数

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| templateID | path | string | yes | — | — |
| nextToken | query | string | no | — | Cursor to start the list from |
| limit | query | integer | no | default=100; minimum=1; maximum=100; format="int32" | Maximum number of items to return per page |

#### 响应

| 状态码 | 说明 | 内容类型 | 结构 |
|---|---|---|---|
| 200 | Successfully returned the template with its builds | application/json | TemplateWithBuilds |
| 401 | Authentication error | application/json | Error |
| 500 | Server error | application/json | Error |

#### 数据结构

##### TemplateWithBuilds

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `templateID` | string | yes | — | Identifier of the template |
| `public` | boolean | yes | — | Whether the template is public or only accessible by the team |
| `aliases` | array of string | yes | deprecated=true | Aliases of the template |
| `names` | array of string | yes | — | Names of the template (namespace/alias format when namespaced) |
| `createdAt` | string | yes | format="date-time" | Time when the template was created |
| `updatedAt` | string | yes | format="date-time" | Time when the template was last updated |
| `lastSpawnedAt` | string / null | yes | format="date-time" | Time when the template was last used |
| `spawnCount` | integer | yes | format="int64" | Number of times the template was used |
| `builds` | array of TemplateBuild | yes | — | List of builds for the template |

##### TemplateBuild

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `buildID` | string | yes | format="uuid" | Identifier of the build |
| `status` | TemplateBuildStatus | yes | enum=["building", "waiting", "ready", "error", "uploaded"] | Status of the template build |
| `createdAt` | string | yes | format="date-time" | Time when the build was created |
| `updatedAt` | string | yes | format="date-time" | Time when the build was last updated |
| `finishedAt` | string | no | format="date-time" | Time when the build was finished |
| `cpuCount` | CPUCount | yes | minimum=1; format="int32" | CPU cores for the sandbox |
| `memoryMB` | MemoryMB | yes | minimum=128; format="int32" | Memory for the sandbox in MiB |
| `diskSizeMB` | DiskSizeMB | no | minimum=0; format="int32" | Disk size for the sandbox in MiB |
| `envdVersion` | EnvdVersion | no | — | Version of the envd running in the sandbox |

##### Error

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |

##### TemplateBuildStatus

Status of the template build

Type: `string`. Constraints: enum=["building", "waiting", "ready", "error", "uploaded"]. See the official contract linked above.

##### CPUCount

CPU cores for the sandbox

Type: `integer`. Constraints: minimum=1; format="int32". See the official contract linked above.

##### MemoryMB

Memory for the sandbox in MiB

Type: `integer`. Constraints: minimum=128; format="int32". See the official contract linked above.

##### DiskSizeMB

Disk size for the sandbox in MiB

Type: `integer`. Constraints: minimum=0; format="int32". See the official contract linked above.

##### EnvdVersion

Version of the envd running in the sandbox

Type: `string`. Constraints: —. See the official contract linked above.



### `GET /v2/templates`



[官方协议来源](https://docs.e2b.dev/api-reference/templates/list-templates-v2)

**Endpoint:** `GET /v2/templates`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

#### 请求参数

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| teamID | query | string | no | — | Identifier of the team |
| nextToken | query | string | no | — | Cursor to start the list from |
| limit | query | integer | no | default=100; minimum=1; maximum=100; format="int32" | Maximum number of items to return per page |

#### 响应

| 状态码 | 说明 | 内容类型 | 结构 |
|---|---|---|---|
| 200 | Successfully returned all templates | application/json | array of Template |
| 400 | Bad request | application/json | Error |
| 401 | Authentication error | application/json | Error |
| 403 | Forbidden | application/json | Error |
| 500 | Server error | application/json | Error |

#### 数据结构

##### Template

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `templateID` | string | yes | — | Identifier of the template |
| `buildID` | string | yes | — | Identifier of the last successful build for given template |
| `cpuCount` | CPUCount | yes | minimum=1; format="int32" | CPU cores for the sandbox |
| `memoryMB` | MemoryMB | yes | minimum=128; format="int32" | Memory for the sandbox in MiB |
| `diskSizeMB` | DiskSizeMB | yes | minimum=0; format="int32" | Disk size for the sandbox in MiB |
| `public` | boolean | yes | — | Whether the template is public or only accessible by the team |
| `aliases` | array of string | yes | deprecated=true | Aliases of the template |
| `names` | array of string | yes | — | Names of the template (namespace/alias format when namespaced) |
| `createdAt` | string | yes | format="date-time" | Time when the template was created |
| `updatedAt` | string | yes | format="date-time" | Time when the template was last updated |
| `createdBy` | oneOf: TeamUser, null | yes | — | — |
| `lastSpawnedAt` | string / null | yes | format="date-time" | Time when the template was last used |
| `spawnCount` | integer | yes | format="int64" | Number of times the template was used |
| `buildCount` | integer | yes | format="int32" | Number of times the template was built |
| `envdVersion` | EnvdVersion | yes | — | Version of the envd running in the sandbox |
| `buildStatus` | TemplateBuildStatus | yes | enum=["building", "waiting", "ready", "error", "uploaded"] | Status of the template build |

##### CPUCount

CPU cores for the sandbox

Type: `integer`. Constraints: minimum=1; format="int32". See the official contract linked above.

##### MemoryMB

Memory for the sandbox in MiB

Type: `integer`. Constraints: minimum=128; format="int32". See the official contract linked above.

##### DiskSizeMB

Disk size for the sandbox in MiB

Type: `integer`. Constraints: minimum=0; format="int32". See the official contract linked above.

##### TeamUser

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `id` | string | yes | format="uuid" | Identifier of the user |
| `email` | string / null | yes | default=null; deprecated=true | Email of the user |

##### EnvdVersion

Version of the envd running in the sandbox

Type: `string`. Constraints: —. See the official contract linked above.

##### TemplateBuildStatus

Status of the template build

Type: `string`. Constraints: enum=["building", "waiting", "ready", "error", "uploaded"]. See the official contract linked above.

##### Error

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |



### `GET /templates`



[官方协议来源](https://docs.e2b.dev/api-reference/templates/list-templates)

**Endpoint:** `GET /templates`

Servers: `https://api.e2b.app`.

Security (upstream OpenAPI): `[{"ApiKeyAuth": []}]`. Scheme details are provided in the linked official contract.

**Deprecated upstream operation.**

#### 请求参数

| Name | Location | Type | Required | Constraints | Description |
|---|---|---|---|---|---|
| teamID | query | string | no | — | Identifier of the team |

#### 响应

| 状态码 | 说明 | 内容类型 | 结构 |
|---|---|---|---|
| 200 | Successfully returned all templates | application/json | array of Template |
| 401 | Authentication error | application/json | Error |
| 500 | Server error | application/json | Error |

#### 数据结构

##### Template

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `templateID` | string | yes | — | Identifier of the template |
| `buildID` | string | yes | — | Identifier of the last successful build for given template |
| `cpuCount` | CPUCount | yes | minimum=1; format="int32" | CPU cores for the sandbox |
| `memoryMB` | MemoryMB | yes | minimum=128; format="int32" | Memory for the sandbox in MiB |
| `diskSizeMB` | DiskSizeMB | yes | minimum=0; format="int32" | Disk size for the sandbox in MiB |
| `public` | boolean | yes | — | Whether the template is public or only accessible by the team |
| `aliases` | array of string | yes | deprecated=true | Aliases of the template |
| `names` | array of string | yes | — | Names of the template (namespace/alias format when namespaced) |
| `createdAt` | string | yes | format="date-time" | Time when the template was created |
| `updatedAt` | string | yes | format="date-time" | Time when the template was last updated |
| `createdBy` | oneOf: TeamUser, null | yes | — | — |
| `lastSpawnedAt` | string / null | yes | format="date-time" | Time when the template was last used |
| `spawnCount` | integer | yes | format="int64" | Number of times the template was used |
| `buildCount` | integer | yes | format="int32" | Number of times the template was built |
| `envdVersion` | EnvdVersion | yes | — | Version of the envd running in the sandbox |
| `buildStatus` | TemplateBuildStatus | yes | enum=["building", "waiting", "ready", "error", "uploaded"] | Status of the template build |

##### CPUCount

CPU cores for the sandbox

Type: `integer`. Constraints: minimum=1; format="int32". See the official contract linked above.

##### MemoryMB

Memory for the sandbox in MiB

Type: `integer`. Constraints: minimum=128; format="int32". See the official contract linked above.

##### DiskSizeMB

Disk size for the sandbox in MiB

Type: `integer`. Constraints: minimum=0; format="int32". See the official contract linked above.

##### TeamUser

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `id` | string | yes | format="uuid" | Identifier of the user |
| `email` | string / null | yes | default=null; deprecated=true | Email of the user |

##### EnvdVersion

Version of the envd running in the sandbox

Type: `string`. Constraints: —. See the official contract linked above.

##### TemplateBuildStatus

Status of the template build

Type: `string`. Constraints: enum=["building", "waiting", "ready", "error", "uploaded"]. See the official contract linked above.

##### Error

| 字段 | 类型 / 结构 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `code` | integer | yes | format="int32" | Error code |
| `error_code` | string | no | — | Machine-readable semantic error code. Not a closed set; initial values: sandbox_capacity_unavailable, sandbox_placement_timeout, sandbox_no_compatible_node, sandbox_create_failed, internal_server_error. |
| `message` | string | yes | — | Error |
